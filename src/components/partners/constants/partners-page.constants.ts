/**
 * Constants for PartnersPage component
 * @module partners/constants/partners-page.constants
 */

/** Delay before opening dialogs to prevent flash (ms) */
export const DIALOG_OPEN_DELAY_MS = 150

/** Tooltip offset from trigger element (px) */
export const TOOLTIP_SIDE_OFFSET = 4

/** Default page size for partners table */
export const DEFAULT_PAGE_SIZE = 10

/** Available page size options */
export const PAGE_SIZE_OPTIONS = [10, 25, 50] as const

/** Initial page number */
export const INITIAL_PAGE = 1
