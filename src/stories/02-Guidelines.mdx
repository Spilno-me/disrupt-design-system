{/* @jsxRuntime automatic */}
{/* @jsxImportSource react */}
import { Meta } from '@storybook/addon-docs/blocks';
import { BookOpen, Eye, Maximize2, Palette, Type } from 'lucide-react';
import {
  BrandHero,
  GuidelineCard,
  InfoBox,
  SectionHeader,
  ABYSS,
  DEEP_CURRENT,
  DUSK_REEF,
  CORAL,
  PRIMITIVES,
  SHADOWS,
  RADIUS,
  SLATE,
  SPACING,
} from './brand/BrandComponents';

<Meta title="Brand/Guidelines" />

{/* Hero Header */}
<BrandHero
  title="Brand Guidelines"
  description="Rules and best practices for using the Disrupt brand consistently across all touchpoints."
  gradient="purple"
  decorativeIcon={<BookOpen size={180} color={PRIMITIVES.white} strokeWidth={1} />}
/>

<InfoBox variant="tip">
  <strong>Consistency is key.</strong> Following these guidelines ensures our brand remains
  recognizable and professional across all platforms and products.
</InfoBox>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Logo Usage</h2>

<SectionHeader
  icon={<Eye size={20} />}
  title="Clear Space"
  description="Always maintain minimum clear space around logos to ensure visibility and impact."
/>

<div style={{
  background: PRIMITIVES.white,
  borderRadius: RADIUS.lg,
  padding: '32px',
  border: `1px solid ${SLATE[200]}`,
  marginBottom: '32px',
}}>
  <div style={{
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    gap: '48px',
  }}>
    <div style={{ textAlign: 'center' }}>
      <div style={{
        background: SLATE[50],
        padding: '48px',
        borderRadius: RADIUS.md,
        position: 'relative',
        marginBottom: '16px',
      }}>
        {/* Clear space indicator */}
        <div style={{
          position: 'absolute',
          top: '16px',
          left: '16px',
          right: '16px',
          bottom: '16px',
          border: `2px dashed ${DEEP_CURRENT[300]}`,
          borderRadius: RADIUS.sm,
        }} />
        <img
          src="/logos/D-pixels-dark-full.svg"
          alt="Disrupt Logo"
          style={{ height: '40px', position: 'relative', zIndex: 1 }}
        />
      </div>
      <div style={{
        fontSize: '13px',
        fontFamily: '"Fixel", sans-serif',
        color: DUSK_REEF[500],
      }}>
        Minimum clear space = height of "D" icon
      </div>
    </div>
  </div>
</div>

### Logo Do's and Don'ts

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(2, 1fr)',
  gap: '20px',
  marginBottom: '48px',
}}>
  <GuidelineCard
    type="do"
    title="Use approved logo files"
    image="/logos/D-pixels-dark-full.svg"
  >
    Always use the official logo files from the brand assets. Never recreate or modify the logo.
  </GuidelineCard>

  {/* Custom Don't card with stretched logo visual */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${CORAL[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      minHeight: '120px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      {/* Stretched logo demonstration */}
      <img
        src="/logos/D-pixels-dark-full.svg"
        alt="Stretched logo - don't do this"
        style={{
          height: '50px',
          transform: 'scaleX(1.6)',
          opacity: 0.9,
        }}
      />
      {/* Red X overlay */}
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: CORAL[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✕</div>
    </div>
    <div style={{ padding: '20px' }}>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px',
        marginBottom: '12px',
      }}>
        <div style={{
          width: '24px',
          height: '24px',
          borderRadius: '50%',
          background: CORAL[500],
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          color: PRIMITIVES.white,
          fontSize: '14px',
          fontWeight: 700,
        }}>✕</div>
        <span style={{
          fontSize: '12px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: CORAL[600],
          textTransform: 'uppercase',
          letterSpacing: '0.5px',
        }}>Don't</span>
      </div>
      <h4 style={{
        margin: '0 0 8px 0',
        fontSize: '14px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: ABYSS[500],
      }}>Stretch or distort</h4>
      <div style={{
        fontSize: '13px',
        fontFamily: '"Fixel", sans-serif',
        color: DUSK_REEF[500],
        lineHeight: 1.5,
      }}>
        Never stretch, compress, or distort the logo. Always maintain the original aspect ratio.
      </div>
    </div>
  </div>

  <GuidelineCard
    type="do"
    title="Use appropriate contrast"
  >
    Use the dark logo on light backgrounds and the light logo on dark backgrounds.
  </GuidelineCard>

  {/* Custom Don't card with effects demonstration */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${CORAL[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      minHeight: '120px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      {/* Logo with effects - shadow and glow */}
      <img
        src="/logos/D-pixels-dark-full.svg"
        alt="Logo with effects - don't do this"
        style={{
          height: '50px',
          filter: 'drop-shadow(4px 4px 8px rgba(0,0,0,0.4)) drop-shadow(0 0 12px rgba(8,164,189,0.6))',
          opacity: 0.9,
        }}
      />
      {/* Red X overlay */}
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: CORAL[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✕</div>
    </div>
    <div style={{ padding: '20px' }}>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px',
        marginBottom: '12px',
      }}>
        <div style={{
          width: '24px',
          height: '24px',
          borderRadius: '50%',
          background: CORAL[500],
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          color: PRIMITIVES.white,
          fontSize: '14px',
          fontWeight: 700,
        }}>✕</div>
        <span style={{
          fontSize: '12px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: CORAL[600],
          textTransform: 'uppercase',
          letterSpacing: '0.5px',
        }}>Don't</span>
      </div>
      <h4 style={{
        margin: '0 0 8px 0',
        fontSize: '14px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: ABYSS[500],
      }}>Add effects</h4>
      <div style={{
        fontSize: '13px',
        fontFamily: '"Fixel", sans-serif',
        color: DUSK_REEF[500],
        lineHeight: 1.5,
      }}>
        Never add shadows, gradients, outlines, or other effects to the logo.
      </div>
    </div>
  </div>
</div>

### Product Logo Do's and Don'ts

The same rules apply to all product logos. Here's how they should NOT be used:

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(3, 1fr)',
  gap: '20px',
  marginBottom: '24px',
}}>
  {/* Flow - Correct */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${DEEP_CURRENT[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      minHeight: '100px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      <img
        src="/logos/flow-logo-full-dark.svg"
        alt="Flow logo - correct usage"
        style={{ height: '36px' }}
      />
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: DEEP_CURRENT[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✓</div>
    </div>
    <div style={{ padding: '16px', textAlign: 'center' }}>
      <span style={{
        fontSize: '12px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: DEEP_CURRENT[600],
        textTransform: 'uppercase',
        letterSpacing: '0.5px',
      }}>Flow - Correct</span>
    </div>
  </div>

  {/* Market - Correct */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${DEEP_CURRENT[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      minHeight: '100px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      <img
        src="/logos/market-logo-full-dark.svg"
        alt="Market logo - correct usage"
        style={{ height: '36px' }}
      />
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: DEEP_CURRENT[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✓</div>
    </div>
    <div style={{ padding: '16px', textAlign: 'center' }}>
      <span style={{
        fontSize: '12px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: DEEP_CURRENT[600],
        textTransform: 'uppercase',
        letterSpacing: '0.5px',
      }}>Market - Correct</span>
    </div>
  </div>

  {/* Partner - Correct */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${DEEP_CURRENT[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      minHeight: '100px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      <img
        src="/logos/partner-logo-full-dark.svg"
        alt="Partner logo - correct usage"
        style={{ height: '36px' }}
      />
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: DEEP_CURRENT[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✓</div>
    </div>
    <div style={{ padding: '16px', textAlign: 'center' }}>
      <span style={{
        fontSize: '12px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: DEEP_CURRENT[600],
        textTransform: 'uppercase',
        letterSpacing: '0.5px',
      }}>Partner - Correct</span>
    </div>
  </div>
</div>

{/* Stretched versions */}
<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(3, 1fr)',
  gap: '20px',
  marginBottom: '24px',
}}>
  {/* Flow - Stretched */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${CORAL[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      minHeight: '100px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      <img
        src="/logos/flow-logo-full-dark.svg"
        alt="Flow logo stretched - don't do this"
        style={{
          height: '36px',
          transform: 'scaleX(1.5)',
          opacity: 0.9,
        }}
      />
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: CORAL[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✕</div>
    </div>
    <div style={{ padding: '16px', textAlign: 'center' }}>
      <span style={{
        fontSize: '12px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: CORAL[600],
        textTransform: 'uppercase',
        letterSpacing: '0.5px',
      }}>Don't stretch</span>
    </div>
  </div>

  {/* Market - Stretched */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${CORAL[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      minHeight: '100px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      <img
        src="/logos/market-logo-full-dark.svg"
        alt="Market logo stretched - don't do this"
        style={{
          height: '36px',
          transform: 'scaleX(1.5)',
          opacity: 0.9,
        }}
      />
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: CORAL[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✕</div>
    </div>
    <div style={{ padding: '16px', textAlign: 'center' }}>
      <span style={{
        fontSize: '12px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: CORAL[600],
        textTransform: 'uppercase',
        letterSpacing: '0.5px',
      }}>Don't stretch</span>
    </div>
  </div>

  {/* Partner - Stretched */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${CORAL[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      minHeight: '100px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      <img
        src="/logos/partner-logo-full-dark.svg"
        alt="Partner logo stretched - don't do this"
        style={{
          height: '36px',
          transform: 'scaleX(1.5)',
          opacity: 0.9,
        }}
      />
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: CORAL[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✕</div>
    </div>
    <div style={{ padding: '16px', textAlign: 'center' }}>
      <span style={{
        fontSize: '12px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: CORAL[600],
        textTransform: 'uppercase',
        letterSpacing: '0.5px',
      }}>Don't stretch</span>
    </div>
  </div>
</div>

{/* Effect versions */}
<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(3, 1fr)',
  gap: '20px',
  marginBottom: '48px',
}}>
  {/* Flow - Effects */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${CORAL[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      minHeight: '100px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      <img
        src="/logos/flow-logo-full-dark.svg"
        alt="Flow logo with effects - don't do this"
        style={{
          height: '36px',
          filter: 'drop-shadow(3px 3px 6px rgba(0,0,0,0.4)) drop-shadow(0 0 10px rgba(8,164,189,0.5))',
          opacity: 0.9,
        }}
      />
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: CORAL[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✕</div>
    </div>
    <div style={{ padding: '16px', textAlign: 'center' }}>
      <span style={{
        fontSize: '12px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: CORAL[600],
        textTransform: 'uppercase',
        letterSpacing: '0.5px',
      }}>Don't add effects</span>
    </div>
  </div>

  {/* Market - Effects */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${CORAL[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      minHeight: '100px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      <img
        src="/logos/market-logo-full-dark.svg"
        alt="Market logo with effects - don't do this"
        style={{
          height: '36px',
          filter: 'drop-shadow(3px 3px 6px rgba(0,0,0,0.4)) drop-shadow(0 0 10px rgba(8,164,189,0.5))',
          opacity: 0.9,
        }}
      />
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: CORAL[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✕</div>
    </div>
    <div style={{ padding: '16px', textAlign: 'center' }}>
      <span style={{
        fontSize: '12px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: CORAL[600],
        textTransform: 'uppercase',
        letterSpacing: '0.5px',
      }}>Don't add effects</span>
    </div>
  </div>

  {/* Partner - Effects */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${CORAL[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      minHeight: '100px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      <img
        src="/logos/partner-logo-full-dark.svg"
        alt="Partner logo with effects - don't do this"
        style={{
          height: '36px',
          filter: 'drop-shadow(3px 3px 6px rgba(0,0,0,0.4)) drop-shadow(0 0 10px rgba(8,164,189,0.5))',
          opacity: 0.9,
        }}
      />
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: CORAL[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✕</div>
    </div>
    <div style={{ padding: '16px', textAlign: 'center' }}>
      <span style={{
        fontSize: '12px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: CORAL[600],
        textTransform: 'uppercase',
        letterSpacing: '0.5px',
      }}>Don't add effects</span>
    </div>
  </div>
</div>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Minimum Sizes</h2>

<SectionHeader
  icon={<Maximize2 size={20} />}
  title="Size Requirements"
  description="Ensure logos remain legible at all sizes."
/>

<div style={{
  background: PRIMITIVES.white,
  borderRadius: RADIUS.lg,
  padding: '32px',
  border: `1px solid ${SLATE[200]}`,
  marginBottom: '48px',
}}>
  <div style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(3, 1fr)',
    gap: '32px',
  }}>
    {[
      { label: 'Print', min: '25mm', desc: 'Minimum width for printed materials' },
      { label: 'Digital', min: '120px', desc: 'Minimum width for web/app usage' },
      { label: 'Favicon', min: '32px', desc: 'Icon only, no wordmark' },
    ].map((item) => (
      <div key={item.label} style={{ textAlign: 'center' }}>
        <div style={{
          background: SLATE[50],
          borderRadius: RADIUS.md,
          padding: '24px',
          marginBottom: '16px',
        }}>
          <div style={{
            fontSize: '32px',
            fontWeight: 700,
            fontFamily: '"Pilat Extended", sans-serif',
            color: ABYSS[500],
          }}>
            {item.min}
          </div>
        </div>
        <div style={{
          fontSize: '14px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: ABYSS[500],
          marginBottom: '4px',
        }}>
          {item.label}
        </div>
        <div style={{
          fontSize: '12px',
          fontFamily: '"Fixel", sans-serif',
          color: DUSK_REEF[500],
        }}>
          {item.desc}
        </div>
      </div>
    ))}
  </div>
</div>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Color Usage</h2>

<SectionHeader
  icon={<Palette size={20} />}
  title="Brand Colors in Context"
  description="How to apply brand colors correctly across different use cases."
/>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(2, 1fr)',
  gap: '20px',
  marginBottom: '32px',
}}>
  <GuidelineCard type="do" title="Use semantic colors">
    Apply colors based on their semantic meaning. Teal for interactive elements, dark for text, purple for accents.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Use colors randomly">
    Don't apply brand colors arbitrarily. Each color has a purpose in the design system.
  </GuidelineCard>

  <GuidelineCard type="do" title="Maintain contrast ratios">
    Ensure text meets WCAG AA standards: 4.5:1 for normal text, 3:1 for large text.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Use low contrast combinations">
    Avoid light text on light backgrounds or dark text on dark backgrounds.
  </GuidelineCard>
</div>

### Primary Color Applications

<div style={{
  background: PRIMITIVES.white,
  borderRadius: RADIUS.lg,
  padding: '24px',
  border: `1px solid ${SLATE[200]}`,
  marginBottom: '48px',
}}>
  <div style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(4, 1fr)',
    gap: '16px',
  }}>
    {[
      { color: ABYSS[500], name: 'Abyss', use: 'Primary text, headings' },
      { color: DEEP_CURRENT[500], name: 'Deep Current', use: 'CTAs, links, accents' },
      { color: DUSK_REEF[500], name: 'Dusk Reef', use: 'Secondary accents' },
      { color: CORAL[500], name: 'Coral', use: 'Errors, warnings' },
    ].map((item) => (
      <div key={item.name} style={{ textAlign: 'center' }}>
        <div style={{
          width: '64px',
          height: '64px',
          borderRadius: RADIUS.md,
          background: item.color,
          margin: '0 auto 12px',
          boxShadow: SHADOWS.sm,
        }} />
        <div style={{
          fontSize: '13px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: ABYSS[500],
          marginBottom: '4px',
        }}>
          {item.name}
        </div>
        <div style={{
          fontSize: '11px',
          fontFamily: '"Fixel", sans-serif',
          color: DUSK_REEF[500],
        }}>
          {item.use}
        </div>
      </div>
    ))}
  </div>
</div>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Typography</h2>

<SectionHeader
  icon={<Type size={20} />}
  title="Font Usage"
  description="Two font families for two contexts."
/>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(2, 1fr)',
  gap: '24px',
  marginBottom: '48px',
}}>
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    padding: '24px',
    border: `1px solid ${SLATE[200]}`,
  }}>
    <div style={{
      fontFamily: '"Pilat Extended", sans-serif',
      fontSize: '28px',
      fontWeight: 700,
      color: ABYSS[500],
      marginBottom: '16px',
    }}>
      Pilat Extended
    </div>
    <div style={{
      fontSize: '13px',
      fontFamily: '"Fixel", sans-serif',
      color: DUSK_REEF[500],
      marginBottom: '16px',
      lineHeight: 1.5,
    }}>
      Use for marketing websites, landing pages, hero sections, and display headings.
    </div>
    <div style={{
      display: 'flex',
      gap: '8px',
    }}>
      <span style={{
        background: DEEP_CURRENT[50],
        color: DEEP_CURRENT[700],
        padding: '4px 10px',
        borderRadius: RADIUS.xs,
        fontSize: '11px',
        fontWeight: 500,
      }}>
        Marketing
      </span>
      <span style={{
        background: DEEP_CURRENT[50],
        color: DEEP_CURRENT[700],
        padding: '4px 10px',
        borderRadius: RADIUS.xs,
        fontSize: '11px',
        fontWeight: 500,
      }}>
        Display
      </span>
    </div>
  </div>

  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    padding: '24px',
    border: `1px solid ${SLATE[200]}`,
  }}>
    <div style={{
      fontFamily: '"Fixel", sans-serif',
      fontSize: '28px',
      fontWeight: 600,
      color: ABYSS[500],
      marginBottom: '16px',
    }}>
      Fixel
    </div>
    <div style={{
      fontSize: '13px',
      fontFamily: '"Fixel", sans-serif',
      color: DUSK_REEF[500],
      marginBottom: '16px',
      lineHeight: 1.5,
    }}>
      Use for applications, dashboards, body text, UI elements, and extended reading.
    </div>
    <div style={{
      display: 'flex',
      gap: '8px',
    }}>
      <span style={{
        background: DUSK_REEF[50],
        color: DUSK_REEF[700],
        padding: '4px 10px',
        borderRadius: RADIUS.xs,
        fontSize: '11px',
        fontWeight: 500,
      }}>
        Application
      </span>
      <span style={{
        background: DUSK_REEF[50],
        color: DUSK_REEF[700],
        padding: '4px 10px',
        borderRadius: RADIUS.xs,
        fontSize: '11px',
        fontWeight: 500,
      }}>
        Body Text
      </span>
    </div>
  </div>
</div>

### Usage Examples

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(2, 1fr)',
  gap: '24px',
  marginBottom: '48px',
}}>
  {/* Marketing/Landing Page Example - Pilat Extended */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${DEEP_CURRENT[200]}`,
  }}>
    <div style={{
      background: `linear-gradient(135deg, ${ABYSS[500]} 0%, ${ABYSS[700]} 100%)`,
      padding: '32px 24px',
      minHeight: '180px',
    }}>
      {/* Mini marketing page mockup */}
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px',
        marginBottom: '20px',
      }}>
        <img
          src="/logos/D-pixels-light-full.svg"
          alt="Disrupt logo"
          style={{ height: '18px' }}
        />
      </div>
      <h2 style={{
        margin: '0 0 10px 0',
        fontSize: '24px',
        fontWeight: 700,
        fontFamily: '"Pilat Extended", sans-serif',
        color: PRIMITIVES.white,
        lineHeight: 1.1,
      }}>
        <span>Transform Your Compliance</span>
      </h2>
      <p style={{
        margin: '0 0 16px 0',
        fontSize: '13px',
        fontFamily: '"Pilat Extended", sans-serif',
        fontWeight: 500,
        color: SLATE[300],
        lineHeight: 1.4,
      }}>
        <span>Streamline reporting and reduce risk.</span>
      </p>
      <div style={{
        display: 'inline-block',
        background: DEEP_CURRENT[500],
        color: PRIMITIVES.white,
        padding: '8px 16px',
        borderRadius: RADIUS.sm,
        fontSize: '12px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
      }}>
        Get Started
      </div>
    </div>
    <div style={{ padding: '14px 20px', background: SLATE[50] }}>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px',
      }}>
        <div style={{
          width: '22px',
          height: '22px',
          borderRadius: '50%',
          background: DEEP_CURRENT[500],
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          color: PRIMITIVES.white,
          fontSize: '11px',
          fontWeight: 700,
        }}>✓</div>
        <span style={{
          fontSize: '12px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: ABYSS[500],
        }}>Pilat Extended for marketing</span>
      </div>
    </div>
  </div>

  {/* Application/Dashboard Example - Fixel */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${DUSK_REEF[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '20px',
      minHeight: '180px',
    }}>
      {/* Mini dashboard mockup */}
      <div style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        marginBottom: '16px',
      }}>
        <h3 style={{
          margin: 0,
          fontSize: '16px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: ABYSS[500],
        }}>
          <span>Dashboard</span>
        </h3>
        <div style={{
          fontSize: '11px',
          fontFamily: '"Fixel", sans-serif',
          color: SLATE[500],
        }}>
          Dec 17, 2024
        </div>
      </div>

      {/* Stats cards */}
      <div style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(2, 1fr)',
        gap: '10px',
        marginBottom: '12px',
      }}>
        <div style={{
          background: PRIMITIVES.white,
          borderRadius: RADIUS.sm,
          padding: '12px',
          border: `1px solid ${SLATE[200]}`,
        }}>
          <div style={{
            fontSize: '10px',
            fontFamily: '"Fixel", sans-serif',
            color: SLATE[500],
            marginBottom: '2px',
          }}>Active Users</div>
          <div style={{
            fontSize: '18px',
            fontWeight: 600,
            fontFamily: '"Fixel", sans-serif',
            color: ABYSS[500],
          }}>2,847</div>
        </div>
        <div style={{
          background: PRIMITIVES.white,
          borderRadius: RADIUS.sm,
          padding: '12px',
          border: `1px solid ${SLATE[200]}`,
        }}>
          <div style={{
            fontSize: '10px',
            fontFamily: '"Fixel", sans-serif',
            color: SLATE[500],
            marginBottom: '2px',
          }}>Compliance</div>
          <div style={{
            fontSize: '18px',
            fontWeight: 600,
            fontFamily: '"Fixel", sans-serif',
            color: DEEP_CURRENT[500],
          }}>94.2%</div>
        </div>
      </div>

      {/* Table preview */}
      <div style={{
        background: PRIMITIVES.white,
        borderRadius: RADIUS.sm,
        border: `1px solid ${SLATE[200]}`,
        overflow: 'hidden',
      }}>
        <div style={{
          display: 'grid',
          gridTemplateColumns: '1fr 1fr',
          gap: '8px',
          padding: '8px 10px',
          borderBottom: `1px solid ${SLATE[200]}`,
          fontSize: '10px',
          fontFamily: '"Fixel", sans-serif',
          fontWeight: 600,
          color: SLATE[500],
        }}>
          <span>Facility</span>
          <span>Status</span>
        </div>
        <div style={{
          display: 'grid',
          gridTemplateColumns: '1fr 1fr',
          gap: '8px',
          padding: '8px 10px',
          fontSize: '11px',
          fontFamily: '"Fixel", sans-serif',
          color: ABYSS[500],
        }}>
          <span>Plant A</span>
          <span style={{ color: DEEP_CURRENT[500] }}>Compliant</span>
        </div>
      </div>
    </div>
    <div style={{ padding: '14px 20px', background: SLATE[100] }}>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px',
      }}>
        <div style={{
          width: '22px',
          height: '22px',
          borderRadius: '50%',
          background: DUSK_REEF[500],
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          color: PRIMITIVES.white,
          fontSize: '11px',
          fontWeight: 700,
        }}>✓</div>
        <span style={{
          fontSize: '12px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: ABYSS[500],
        }}>Fixel for applications</span>
      </div>
    </div>
  </div>
</div>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Co-Branding</h2>

<InfoBox variant="warning">
  <strong>Partner logos</strong> should always be separated from the Disrupt logo with clear visual distinction.
  Contact the brand team for co-branding approval.
</InfoBox>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(2, 1fr)',
  gap: '20px',
}}>
  {/* DO: Use dividers - Visual example */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${DEEP_CURRENT[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '32px 24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      gap: '24px',
      minHeight: '120px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      {/* Disrupt logo */}
      <img
        src="/logos/D-pixels-dark-full.svg"
        alt="Disrupt logo"
        style={{ height: '32px' }}
      />
      {/* Vertical divider */}
      <div style={{
        width: '1px',
        height: '40px',
        background: SLATE[300],
      }} />
      {/* Flow logo */}
      <img
        src="/logos/flow-logo-full-dark.svg"
        alt="Flow logo"
        style={{ height: '28px' }}
      />
      {/* Check badge */}
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: DEEP_CURRENT[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✓</div>
    </div>
    <div style={{ padding: '20px' }}>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px',
        marginBottom: '12px',
      }}>
        <div style={{
          width: '24px',
          height: '24px',
          borderRadius: '50%',
          background: DEEP_CURRENT[500],
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          color: PRIMITIVES.white,
          fontSize: '14px',
          fontWeight: 700,
        }}>✓</div>
        <span style={{
          fontSize: '12px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: DEEP_CURRENT[600],
          textTransform: 'uppercase',
          letterSpacing: '0.5px',
        }}>Do</span>
      </div>
      <h4 style={{
        margin: '0 0 8px 0',
        fontSize: '14px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: ABYSS[500],
      }}>Use dividers</h4>
      <div style={{
        fontSize: '13px',
        fontFamily: '"Fixel", sans-serif',
        color: DUSK_REEF[500],
        lineHeight: 1.5,
      }}>
        When displaying multiple logos, use a vertical divider or adequate spacing between them.
      </div>
    </div>
  </div>

  {/* DON'T: Merge or combine logos - Visual example */}
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.lg,
    overflow: 'hidden',
    border: `1px solid ${CORAL[200]}`,
  }}>
    <div style={{
      background: SLATE[50],
      padding: '32px 24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      minHeight: '120px',
      borderBottom: `1px solid ${SLATE[200]}`,
      position: 'relative',
    }}>
      {/* Logos placed too close without separator */}
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px',
      }}>
        <img
          src="/logos/D-pixels-dark-full.svg"
          alt="Disrupt logo without separator - don't do this"
          style={{ height: '32px' }}
        />
        <img
          src="/logos/flow-logo-full-dark.svg"
          alt="Flow logo without separator - don't do this"
          style={{ height: '28px' }}
        />
      </div>
      {/* X badge */}
      <div style={{
        position: 'absolute',
        top: '8px',
        right: '8px',
        width: '24px',
        height: '24px',
        borderRadius: '50%',
        background: CORAL[500],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: PRIMITIVES.white,
        fontSize: '14px',
        fontWeight: 700,
      }}>✕</div>
    </div>
    <div style={{ padding: '20px' }}>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px',
        marginBottom: '12px',
      }}>
        <div style={{
          width: '24px',
          height: '24px',
          borderRadius: '50%',
          background: CORAL[500],
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          color: PRIMITIVES.white,
          fontSize: '14px',
          fontWeight: 700,
        }}>✕</div>
        <span style={{
          fontSize: '12px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: CORAL[600],
          textTransform: 'uppercase',
          letterSpacing: '0.5px',
        }}>Don't</span>
      </div>
      <h4 style={{
        margin: '0 0 8px 0',
        fontSize: '14px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: ABYSS[500],
      }}>Place logos without separation</h4>
      <div style={{
        fontSize: '13px',
        fontFamily: '"Fixel", sans-serif',
        color: DUSK_REEF[500],
        lineHeight: 1.5,
      }}>
        Never place logos side by side without a clear visual divider between them.
      </div>
    </div>
  </div>
</div>
