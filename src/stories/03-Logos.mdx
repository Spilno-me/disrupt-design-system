{/* @jsxRuntime automatic */}
{/* @jsxImportSource react */}
import { Meta } from '@storybook/addon-docs/blocks';
import { Image, Moon, Sun, Maximize2 } from 'lucide-react';
import { LogoGallery } from './LogoGallery';
import {
  BrandHero,
  InfoBox,
  ABYSS,
  DEEP_CURRENT,
  DUSK_REEF,
  PRIMITIVES,
  SHADOWS,
  RADIUS,
  SLATE,
  SPACING,
} from './brand/BrandComponents';

<Meta title="Brand/Logos" />

{/* Hero Header */}
<BrandHero
  title="Brand Logos"
  description="Download official Disrupt brand logos in various formats. Select the variant and format you need."
  gradient="teal"
  decorativeIcon={<Image size={180} color={PRIMITIVES.white} strokeWidth={1} />}
/>

<InfoBox variant="tip">
  <strong>Quick Export:</strong> Select your preferred format (PNG or SVG) and scale,
  then click Export to download. SVG is recommended for most uses.
</InfoBox>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Logo Gallery</h2>

Download logos for Disrupt and all product brands. Each logo is available in dark and light variants.

<LogoGallery />

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Usage Guidelines</h2>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(3, 1fr)',
  gap: '16px',
  marginTop: '24px',
}}>
  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.md,
    padding: '20px',
    border: `1px solid ${SLATE[200]}`,
  }}>
    <div style={{
      width: '40px',
      height: '40px',
      borderRadius: RADIUS.sm,
      background: ABYSS[50],
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      marginBottom: '12px',
    }}>
      <Moon size={20} color={ABYSS[500]} />
    </div>
    <h4 style={{
      margin: '0 0 8px 0',
      fontSize: '14px',
      fontWeight: 600,
      fontFamily: '"Fixel", sans-serif',
      color: ABYSS[500],
    }}>
      <span>Dark Variant</span>
    </h4>
    <div style={{
      margin: 0,
      fontSize: '13px',
      fontFamily: '"Fixel", sans-serif',
      color: DUSK_REEF[500],
      lineHeight: 1.5,
    }}>
      <span>Use on light backgrounds (white, cream, light gray)</span>
    </div>
  </div>

  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.md,
    padding: '20px',
    border: `1px solid ${SLATE[200]}`,
  }}>
    <div style={{
      width: '40px',
      height: '40px',
      borderRadius: RADIUS.sm,
      background: DEEP_CURRENT[50],
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      marginBottom: '12px',
    }}>
      <Sun size={20} color={DEEP_CURRENT[600]} />
    </div>
    <h4 style={{
      margin: '0 0 8px 0',
      fontSize: '14px',
      fontWeight: 600,
      fontFamily: '"Fixel", sans-serif',
      color: ABYSS[500],
    }}>
      <span>Light Variant</span>
    </h4>
    <div style={{
      margin: 0,
      fontSize: '13px',
      fontFamily: '"Fixel", sans-serif',
      color: DUSK_REEF[500],
      lineHeight: 1.5,
    }}>
      <span>Use on dark backgrounds (navy, black, dark images)</span>
    </div>
  </div>

  <div style={{
    background: PRIMITIVES.white,
    borderRadius: RADIUS.md,
    padding: '20px',
    border: `1px solid ${SLATE[200]}`,
  }}>
    <div style={{
      width: '40px',
      height: '40px',
      borderRadius: RADIUS.sm,
      background: DUSK_REEF[50],
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      marginBottom: '12px',
    }}>
      <Maximize2 size={20} color={DUSK_REEF[600]} />
    </div>
    <h4 style={{
      margin: '0 0 8px 0',
      fontSize: '14px',
      fontWeight: 600,
      fontFamily: '"Fixel", sans-serif',
      color: ABYSS[500],
    }}>
      <span>Clear Space</span>
    </h4>
    <div style={{
      margin: 0,
      fontSize: '13px',
      fontFamily: '"Fixel", sans-serif',
      color: DUSK_REEF[500],
      lineHeight: 1.5,
    }}>
      <span>Always maintain minimum padding equal to the "D" icon height</span>
    </div>
  </div>
</div>
