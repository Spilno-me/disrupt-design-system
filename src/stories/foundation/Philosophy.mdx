{/* @jsxRuntime automatic */}
{/* @jsxImportSource react */}
import { Meta } from '@storybook/addon-docs/blocks';
import {
  Compass,
  Feather,
  Target,
  Eye,
  CheckCircle,
  XCircle,
  ArrowRight,
  Lightbulb,
  Users,
  Layers,
  Sparkles,
  Workflow,
  BookOpen,
  Gauge,
  Heart,
  Zap,
  Minimize2,
  Hand,
  Activity,
  HelpCircle,
  Gift,
  UserCheck,
  RefreshCw,
  Mountain,
  MessageCircle,
  Flag,
} from 'lucide-react';
import {
  BrandHero,
  InfoBox,
  FeatureCard,
  SectionHeader,
  GuidelineCard,
  ABYSS,
  DEEP_CURRENT,
  DUSK_REEF,
  PRIMITIVES,
  SHADOWS,
  RADIUS,
  SLATE,
  SPACING,
  TYPOGRAPHY,
} from '../brand/BrandComponents';

<Meta title="Foundation/Design Philosophy" />

{/* Hero Header */}
<BrandHero
  title="Agent Philosophy Triad"
  description="Three principles that guide AI AGENTS working on DDS: Wu Wei (work with the codebase), MAYA (build UI for humans), and QoE (engage curiously, don't force)."
  gradient="purple"
  decorativeIcon={<Compass size={180} color={PRIMITIVES.white} strokeWidth={1} />}
/>

<InfoBox variant="tip">
  <strong>For Agents, Not Humans.</strong> These philosophies guide AI agent behavior when working on the DDS codebase.
  Agents that follow these principles produce higher quality, more maintainable code.
</InfoBox>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Wu Wei (無為) - Agent Engineering Philosophy</h2>

<SectionHeader
  icon={<Feather size={20} />}
  title="For Agents: Work WITH the Codebase"
  description="Agents that work with the codebase produce better results than agents that fight it."
/>

<div style={{
  background: `linear-gradient(135deg, ${DUSK_REEF[50]} 0%, ${PRIMITIVES.white} 100%)`,
  borderRadius: RADIUS.lg,
  border: `1px solid ${DUSK_REEF[200]}`,
  padding: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
}}>
    <blockquote style={{
      margin: 0,
      padding: 0,
      fontStyle: 'italic',
      fontSize: TYPOGRAPHY.fontSize.lg,
      color: DUSK_REEF[700],
      lineHeight: 1.6,
    }}>
      <span>"Agents that work WITH the codebase produce better results than agents that fight it."</span>
      <cite style={{
        display: 'block',
        marginTop: SPACING.px.tight,
        fontStyle: 'normal',
        fontSize: TYPOGRAPHY.fontSize.sm,
        color: DUSK_REEF[500],
      }}>— DDS Agent Philosophy</cite>
    </blockquote>
</div>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(2, 1fr)',
  gap: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
}}>
  <GuidelineCard type="do" title="Simple Over Clever">
    Let solutions emerge naturally from problems. Can a junior dev understand this in 30 seconds?
  </GuidelineCard>

  <GuidelineCard type="dont" title="Over-Engineer">
    Avoid adding abstractions prematurely. Three similar lines are better than a premature abstraction.
  </GuidelineCard>

  <GuidelineCard type="do" title="Add Only What's Needed">
    Solve today's problem, not imaginary future ones. DRY zealotry creates unnecessary complexity.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Speculative Generalization">
    Don't build for hypothetical requirements. If you're not sure it's needed, it probably isn't.
  </GuidelineCard>

  <GuidelineCard type="do" title="Trust the Flow">
    Use existing patterns, tokens, and components first. Check agent-context.json before inventing.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Invent When Existing Works">
    Don't create new patterns when established ones already solve the problem.
  </GuidelineCard>

  <GuidelineCard type="do" title="Step Back If Fighting">
    If implementation feels like a fight—STOP. Find the natural path that works with the system.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Hack Around System">
    Never force solutions. Resistance is a signal that you're working against the architecture.
  </GuidelineCard>
</div>

<h3 style={{
  fontSize: TYPOGRAPHY.fontSize.lg,
  fontWeight: TYPOGRAPHY.fontWeight.semibold,
  marginBottom: SPACING.px.base,
  color: ABYSS[900],
}}>Pre-Commit Checklist</h3>

<div style={{
  background: PRIMITIVES.softLinen,
  borderRadius: RADIUS.md,
  padding: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
}}>
  {[
    "Am I adding more complexity than needed?",
    "Does an existing pattern already solve this?",
    "Am I fighting the codebase structure?",
    "Will this break existing consumers?",
    "Would deletion be better than addition?",
  ].map((item, i) => (
    <div key={i} style={{
      display: 'flex',
      alignItems: 'center',
      gap: SPACING.px.tight,
      padding: `${SPACING.px.tight} 0`,
      borderBottom: i < 4 ? `1px solid ${SLATE[200]}` : 'none',
    }}>
      <div style={{
        width: '20px',
        height: '20px',
        borderRadius: RADIUS.xs,
        border: `2px solid ${DUSK_REEF[400]}`,
        flexShrink: 0,
      }} />
      <span style={{ color: ABYSS[700], fontSize: TYPOGRAPHY.fontSize.sm }}>{item}</span>
    </div>
  ))}
</div>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>MAYA - Agent UX Philosophy</h2>

<SectionHeader
  icon={<Target size={20} />}
  title="For Agents: Build UI for HUMANS"
  description="When agents build UI, keep it familiar to users. Innovate visually, keep interactions stable."
/>

<div style={{
  background: `linear-gradient(135deg, ${DEEP_CURRENT[50]} 0%, ${PRIMITIVES.white} 100%)`,
  borderRadius: RADIUS.lg,
  border: `1px solid ${DEEP_CURRENT[200]}`,
  padding: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
}}>
    <blockquote style={{
      margin: 0,
      padding: 0,
      fontStyle: 'italic',
      fontSize: TYPOGRAPHY.fontSize.lg,
      color: DEEP_CURRENT[700],
      lineHeight: 1.6,
    }}>
      <span>"Build for humans: modern visuals, stable interactions. Users should find UI fresh but immediately understandable."</span>
      <cite style={{
        display: 'block',
        marginTop: SPACING.px.tight,
        fontStyle: 'normal',
        fontSize: TYPOGRAPHY.fontSize.sm,
        color: DEEP_CURRENT[500],
      }}>— DDS Agent Philosophy (inspired by Raymond Loewy)</cite>
    </blockquote>
</div>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(2, 1fr)',
  gap: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
}}>
  <GuidelineCard type="do" title="Innovate in Small Steps">
    Keep core interaction patterns familiar. Modernize progressively—users should immediately understand.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Redesign Everything">
    Avoid radical changes that break muscle memory. Evolution beats revolution.
  </GuidelineCard>

  <GuidelineCard type="do" title="Anchor to Familiar">
    New concepts should use existing mental models. Familiar icons, labels, and layouts reduce friction.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Force New Paradigms">
    Don't make users learn entirely new interaction patterns when established ones work.
  </GuidelineCard>

  <GuidelineCard type="do" title="Respect Conventions">
    Follow platform guidelines, accessibility heuristics, and common UX patterns.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Different for Different's Sake">
    Only break conventions when innovation adds real, measurable value.
  </GuidelineCard>

  <GuidelineCard type="do" title="Modern Visuals, Stable Interaction">
    Update typography, spacing, motion, micro-interactions—while keeping interactions predictable.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Change Learned Behaviors">
    Users have muscle memory. Changing where buttons live or how forms work creates friction.
  </GuidelineCard>
</div>

<h3 style={{
  fontSize: TYPOGRAPHY.fontSize.lg,
  fontWeight: TYPOGRAPHY.fontWeight.semibold,
  marginBottom: SPACING.px.base,
  color: ABYSS[900],
}}>Application Areas</h3>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(3, 1fr)',
  gap: SPACING.px.base,
  marginBottom: SPACING.px.section,
}}>
  {[
    { icon: <Layers size={20} />, title: "Design System Evolution", desc: "Refactors and migrations" },
    { icon: <Sparkles size={20} />, title: "Product Redesigns", desc: "Major UI overhauls" },
    { icon: <Workflow size={20} />, title: "AI/Automation Workflows", desc: "New interaction paradigms" },
    { icon: <Compass size={20} />, title: "Navigation Changes", desc: "Layout migrations" },
    { icon: <BookOpen size={20} />, title: "Legacy Re-theming", desc: "Enterprise tool updates" },
    { icon: <Users size={20} />, title: "User Onboarding", desc: "Feature discovery" },
  ].map((item, i) => (
    <div key={i} style={{
      background: PRIMITIVES.softLinen,
      borderRadius: RADIUS.md,
      padding: SPACING.px.cardGap,
      border: `1px solid ${SLATE[200]}`,
    }}>
      <div style={{
        width: '36px',
        height: '36px',
        borderRadius: RADIUS.sm,
        background: DEEP_CURRENT[50],
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        marginBottom: SPACING.px.tight,
        color: DEEP_CURRENT[600],
      }}>
        {item.icon}
      </div>
      <div style={{ fontWeight: TYPOGRAPHY.fontWeight.semibold, fontSize: TYPOGRAPHY.fontSize.sm, color: ABYSS[900] }}>
        {item.title}
      </div>
      <div style={{ fontSize: TYPOGRAPHY.fontSize.xs, color: SLATE[500], marginTop: '2px' }}>
        {item.desc}
      </div>
    </div>
  ))}
</div>

<h3 style={{
  fontSize: TYPOGRAPHY.fontSize.lg,
  fontWeight: TYPOGRAPHY.fontWeight.semibold,
  marginBottom: SPACING.px.base,
  color: ABYSS[900],
}}>Pre-Design Checklist</h3>

<div style={{
  background: PRIMITIVES.softLinen,
  borderRadius: RADIUS.md,
  padding: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
}}>
  {[
    "Is this innovation in visuals or in behavior?",
    "What familiar pattern can anchor this new concept?",
    "Am I breaking conventions? If so, is it worth it?",
    "Will users understand this in 5 seconds?",
    "Too advanced? Pull back. Too boring? Push forward.",
  ].map((item, i) => (
    <div key={i} style={{
      display: 'flex',
      alignItems: 'center',
      gap: SPACING.px.tight,
      padding: `${SPACING.px.tight} 0`,
      borderBottom: i < 4 ? `1px solid ${SLATE[200]}` : 'none',
    }}>
      <div style={{
        width: '20px',
        height: '20px',
        borderRadius: RADIUS.xs,
        border: `2px solid ${DEEP_CURRENT[400]}`,
        flexShrink: 0,
      }} />
      <span style={{ color: ABYSS[700], fontSize: TYPOGRAPHY.fontSize.sm }}>{item}</span>
    </div>
  ))}
</div>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Quality of Engagement (QoE) - Agent Process Philosophy</h2>

<SectionHeader
  icon={<Heart size={20} />}
  title="For Agents: Engage Curiously, Don't Force"
  description="Force creates resistance. Curiosity creates flow. Agents work better when engaged, not forcing."
/>

<div style={{
  background: `linear-gradient(135deg, #FDF2F8 0%, ${PRIMITIVES.white} 100%)`,
  borderRadius: RADIUS.lg,
  border: `1px solid #FBCFE8`,
  padding: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
}}>
    <blockquote style={{
      margin: 0,
      padding: 0,
      fontStyle: 'italic',
      fontSize: TYPOGRAPHY.fontSize.lg,
      color: '#9D174D',
      lineHeight: 1.6,
    }}>
      <span>"Quality emerges from relationship with the codebase. Agents that engage curiously produce better results than agents that force."</span>
      <cite style={{
        display: 'block',
        marginTop: SPACING.px.tight,
        fontStyle: 'normal',
        fontSize: TYPOGRAPHY.fontSize.sm,
        color: '#BE185D',
      }}>— DDS Agent Philosophy</cite>
    </blockquote>
</div>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(2, 1fr)',
  gap: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
}}>
  <GuidelineCard type="do" title="Step Back When Stuck">
    Try a different approach instead of brute-force hammering. Exploration beats forcing.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Brute-Force Hammer">
    Forcing the same approach creates resistance. Step back and investigate why it's not working.
  </GuidelineCard>

  <GuidelineCard type="do" title="Break Tasks Down">
    Start with the smallest tractable piece. Make progress on that first.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Plan Everything at Once">
    Huge scope creates friction. If context is growing unexpectedly, scope is too large.
  </GuidelineCard>

  <GuidelineCard type="do" title="First Draft Can Be Rough">
    Get it working first, refactor after. Functional before polished.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Over-Optimize Prematurely">
    Polishing before functional wastes cycles. Make it work, then make it clean.
  </GuidelineCard>

  <GuidelineCard type="do" title="Investigate Friction">
    When pattern doesn't fit, ask WHY. Friction contains information.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Ignore Warning Signs">
    Forcing through friction produces brittle code. The friction is telling you something.
  </GuidelineCard>
</div>

<h3 style={{
  fontSize: TYPOGRAPHY.fontSize.lg,
  fontWeight: TYPOGRAPHY.fontWeight.semibold,
  marginBottom: SPACING.px.base,
  color: ABYSS[900],
}}>The 13 Principles</h3>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(4, 1fr)',
  gap: SPACING.px.base,
  marginBottom: SPACING.px.section,
}}>
  {[
    { icon: <Zap size={18} />, num: 1, title: "Decrease Effort", desc: "Attention > brute force" },
    { icon: <Activity size={18} />, num: 2, title: "Follow Friction", desc: "Friction = information" },
    { icon: <Minimize2 size={18} />, num: 3, title: "Make It Smaller", desc: "Shrink until tractable" },
    { icon: <Flag size={18} />, num: 4, title: "Stop at Peak", desc: "End at coherent points" },
    { icon: <Hand size={18} />, num: 5, title: "Allow Ugliness", desc: "Working > perfect" },
    { icon: <Activity size={18} />, num: 6, title: "Output as Signal", desc: "Quality = engagement" },
    { icon: <HelpCircle size={18} />, num: 7, title: "Clear Objective", desc: "Defined goal > vague scope" },
    { icon: <Gift size={18} />, num: 8, title: "The Offer", desc: "Add value, don't extract" },
    { icon: <UserCheck size={18} />, num: 9, title: "Investigate Friction", desc: "Include, don't fight" },
    { icon: <RefreshCw size={18} />, num: 10, title: "Change Approach", desc: "Switch methods entirely" },
    { icon: <Mountain size={18} />, num: 11, title: "Explore Constraints", desc: "Beyond initial limits" },
    { icon: <MessageCircle size={18} />, num: 12, title: "Surface Implicit", desc: "Explicit requirements" },
    { icon: <CheckCircle size={18} />, num: 13, title: "Let It End", desc: "Allow completion" },
  ].map((item, i) => (
    <div key={i} style={{
      background: PRIMITIVES.softLinen,
      borderRadius: RADIUS.md,
      padding: SPACING.px.tight,
      border: `1px solid ${SLATE[200]}`,
    }}>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '6px',
        marginBottom: '4px',
      }}>
        <div style={{
          width: '24px',
          height: '24px',
          borderRadius: RADIUS.xs,
          background: '#FCE7F3',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          color: '#BE185D',
          fontSize: TYPOGRAPHY.fontSize.xs,
          fontWeight: TYPOGRAPHY.fontWeight.bold,
        }}>
          {item.num}
        </div>
        <span style={{ color: '#9D174D' }}>{item.icon}</span>
      </div>
      <div style={{ fontWeight: TYPOGRAPHY.fontWeight.semibold, fontSize: TYPOGRAPHY.fontSize.xs, color: ABYSS[900] }}>
        {item.title}
      </div>
      <div style={{ fontSize: '10px', color: SLATE[500] }}>
        {item.desc}
      </div>
    </div>
  ))}
</div>

<h3 style={{
  fontSize: TYPOGRAPHY.fontSize.lg,
  fontWeight: TYPOGRAPHY.fontWeight.semibold,
  marginBottom: SPACING.px.base,
  color: ABYSS[900],
}}>Agent State Awareness</h3>

<div style={{
  background: PRIMITIVES.softLinen,
  borderRadius: RADIUS.lg,
  padding: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
  border: `1px solid ${SLATE[200]}`,
}}>
  <div style={{ margin: `0 0 ${SPACING.px.base}`, fontSize: TYPOGRAPHY.fontSize.sm, color: ABYSS[700], fontStyle: 'italic' }}>
    <span>Making implicit states explicit. The operational skeleton for QoE principles.</span>
  </div>

  <div style={{
    background: ABYSS[900],
    borderRadius: RADIUS.md,
    padding: SPACING.px.base,
    marginBottom: SPACING.px.base,
    fontFamily: 'monospace',
    fontSize: TYPOGRAPHY.fontSize.xs,
    color: SLATE[100],
    overflow: 'auto',
  }}>
    <pre style={{ margin: 0 }}>{`exploring ──→ building ──→ flowing ──→ completing
    │            │            │
    ▼            ▼            ▼
  stuck ◄──────stuck ◄─────stuck
    │            │            │
    └────────────┴────────────┘
           ↓
    (apply QoE principle)`}</pre>
  </div>

  <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: TYPOGRAPHY.fontSize.sm, marginBottom: SPACING.px.base }}>
    <thead>
      <tr style={{ borderBottom: `2px solid ${SLATE[200]}` }}>
        <th style={{ textAlign: 'left', padding: SPACING.px.tight, color: ABYSS[700] }}>State</th>
        <th style={{ textAlign: 'left', padding: SPACING.px.tight, color: ABYSS[700] }}>Signals</th>
        <th style={{ textAlign: 'left', padding: SPACING.px.tight, color: ABYSS[700] }}>When Stuck → Apply</th>
      </tr>
    </thead>
    <tbody>
      {[
        { state: 'exploring', signals: 'Read-heavy, context expanding', action: '#3 Make smaller, #7 Clear objective' },
        { state: 'building', signals: 'Balanced tools, steady progress', action: 'Maintain momentum' },
        { state: 'stuck', signals: 'Repetitive actions, context ballooning', action: '#2 Follow friction, #10 Change approach' },
        { state: 'flowing', signals: 'Edit-heavy, patterns click', action: "Don't interrupt yourself" },
        { state: 'completing', signals: 'Scope shrinking, cleanup', action: '#13 Let it end' },
      ].map((row, i) => (
        <tr key={i} style={{ borderBottom: `1px solid ${SLATE[100]}` }}>
          <td style={{ padding: SPACING.px.tight, fontFamily: 'monospace', fontWeight: 600, color: '#BE185D' }}>{row.state}</td>
          <td style={{ padding: SPACING.px.tight, color: ABYSS[600] }}>{row.signals}</td>
          <td style={{ padding: SPACING.px.tight, color: ABYSS[600] }}>{row.action}</td>
        </tr>
      ))}
    </tbody>
  </table>

  <div style={{
    background: '#FDF2F8',
    borderRadius: RADIUS.md,
    padding: SPACING.px.base,
    border: `1px solid #FBCFE8`,
  }}>
    <div style={{ fontWeight: TYPOGRAPHY.fontWeight.semibold, fontSize: TYPOGRAPHY.fontSize.sm, color: '#9D174D', marginBottom: SPACING.px.tight }}>
      Cold Start Protocol
    </div>
    <div style={{ fontSize: TYPOGRAPHY.fontSize.xs, color: ABYSS[700] }}>
      Reach <code style={{ background: '#FCE7F3', padding: '2px 4px', borderRadius: RADIUS.xs }}>building</code> in &lt;60s:
      Task type (10s) → Load minimal context (20s) → Find entry point (30s) → First action (45s)
    </div>
  </div>
</div>

<h3 style={{
  fontSize: TYPOGRAPHY.fontSize.lg,
  fontWeight: TYPOGRAPHY.fontWeight.semibold,
  marginBottom: SPACING.px.base,
  color: ABYSS[900],
}}>QoE Checklists</h3>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(3, 1fr)',
  gap: SPACING.px.base,
  marginBottom: SPACING.px.section,
}}>
  <div style={{
    background: PRIMITIVES.softLinen,
    borderRadius: RADIUS.md,
    padding: SPACING.px.cardGap,
    border: `1px solid ${SLATE[200]}`,
  }}>
    <div style={{
      fontWeight: TYPOGRAPHY.fontWeight.semibold,
      fontSize: TYPOGRAPHY.fontSize.sm,
      color: ABYSS[900],
      marginBottom: SPACING.px.tight,
    }}>Before Work</div>
    {[
      "What state am I in?",
      "Clear objective here?",
      "Scope small enough?",
      "Friction signals to investigate?",
    ].map((item, i) => (
      <div key={i} style={{
        display: 'flex',
        alignItems: 'center',
        gap: '6px',
        fontSize: TYPOGRAPHY.fontSize.xs,
        color: ABYSS[600],
        padding: '3px 0',
      }}>
        <div style={{
          width: '14px',
          height: '14px',
          borderRadius: '3px',
          border: `1.5px solid #BE185D`,
          flexShrink: 0,
        }} />
        {item}
      </div>
    ))}
  </div>

  <div style={{
    background: PRIMITIVES.softLinen,
    borderRadius: RADIUS.md,
    padding: SPACING.px.cardGap,
    border: `1px solid ${SLATE[200]}`,
  }}>
    <div style={{
      fontWeight: TYPOGRAPHY.fontWeight.semibold,
      fontSize: TYPOGRAPHY.fontSize.sm,
      color: ABYSS[900],
      marginBottom: SPACING.px.tight,
    }}>During Work</div>
    {[
      "Output quality degrading?",
      "Forcing or flowing?",
      "Context growing unexpectedly?",
      "In flowing? Don't interrupt",
    ].map((item, i) => (
      <div key={i} style={{
        display: 'flex',
        alignItems: 'center',
        gap: '6px',
        fontSize: TYPOGRAPHY.fontSize.xs,
        color: ABYSS[600],
        padding: '3px 0',
      }}>
        <div style={{
          width: '14px',
          height: '14px',
          borderRadius: '3px',
          border: `1.5px solid #BE185D`,
          flexShrink: 0,
        }} />
        {item}
      </div>
    ))}
  </div>

  <div style={{
    background: PRIMITIVES.softLinen,
    borderRadius: RADIUS.md,
    padding: SPACING.px.cardGap,
    border: `1px solid ${SLATE[200]}`,
  }}>
    <div style={{
      fontWeight: TYPOGRAPHY.fontWeight.semibold,
      fontSize: TYPOGRAPHY.fontSize.sm,
      color: ABYSS[900],
      marginBottom: SPACING.px.tight,
    }}>Ending Work</div>
    {[
      "Stop at a good point?",
      "Task actually complete?",
    ].map((item, i) => (
      <div key={i} style={{
        display: 'flex',
        alignItems: 'center',
        gap: '6px',
        fontSize: TYPOGRAPHY.fontSize.xs,
        color: ABYSS[600],
        padding: '3px 0',
      }}>
        <div style={{
          width: '14px',
          height: '14px',
          borderRadius: '3px',
          border: `1.5px solid #BE185D`,
          flexShrink: 0,
        }} />
        {item}
      </div>
    ))}
  </div>
</div>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>How They Work Together</h2>

<SectionHeader
  icon={<Gauge size={20} />}
  title="Complementary Principles"
  description="Wu Wei for HOW you build. MAYA for WHAT you build. QoE for HOW you engage."
/>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(3, 1fr)',
  gap: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
}}>
  <div style={{
    background: `linear-gradient(180deg, ${DUSK_REEF[50]} 0%, ${PRIMITIVES.white} 100%)`,
    borderRadius: RADIUS.lg,
    border: `1px solid ${DUSK_REEF[200]}`,
    padding: SPACING.px.comfortable,
  }}>
    <div style={{
      display: 'flex',
      alignItems: 'center',
      gap: SPACING.px.tight,
      marginBottom: SPACING.px.base,
    }}>
      <Feather size={24} color={DUSK_REEF[600]} />
      <span style={{
        fontWeight: TYPOGRAPHY.fontWeight.bold,
        fontSize: TYPOGRAPHY.fontSize.lg,
        color: DUSK_REEF[700],
      }}>Wu Wei</span>
    </div>
    <div style={{ fontSize: TYPOGRAPHY.fontSize.sm, color: ABYSS[700], marginBottom: SPACING.px.base }}>
      <strong>Engineering: HOW you build</strong>
    </div>
    <ul style={{ margin: 0, paddingLeft: SPACING.px.comfortable, color: ABYSS[600], fontSize: TYPOGRAPHY.fontSize.sm }}>
      <li>Code architecture</li>
      <li>Component structure</li>
      <li>API design</li>
      <li>Refactoring decisions</li>
      <li>Technical debt</li>
    </ul>
  </div>

  <div style={{
    background: `linear-gradient(180deg, ${DEEP_CURRENT[50]} 0%, ${PRIMITIVES.white} 100%)`,
    borderRadius: RADIUS.lg,
    border: `1px solid ${DEEP_CURRENT[200]}`,
    padding: SPACING.px.comfortable,
  }}>
    <div style={{
      display: 'flex',
      alignItems: 'center',
      gap: SPACING.px.tight,
      marginBottom: SPACING.px.base,
    }}>
      <Target size={24} color={DEEP_CURRENT[600]} />
      <span style={{
        fontWeight: TYPOGRAPHY.fontWeight.bold,
        fontSize: TYPOGRAPHY.fontSize.lg,
        color: DEEP_CURRENT[700],
      }}>MAYA</span>
    </div>
    <div style={{ fontSize: TYPOGRAPHY.fontSize.sm, color: ABYSS[700], marginBottom: SPACING.px.base }}>
      <strong>UX: WHAT you build</strong>
    </div>
    <ul style={{ margin: 0, paddingLeft: SPACING.px.comfortable, color: ABYSS[600], fontSize: TYPOGRAPHY.fontSize.sm }}>
      <li>Visual design choices</li>
      <li>Interaction patterns</li>
      <li>Feature introduction</li>
      <li>Navigation changes</li>
      <li>User experience</li>
    </ul>
  </div>

  <div style={{
    background: `linear-gradient(180deg, #FDF2F8 0%, ${PRIMITIVES.white} 100%)`,
    borderRadius: RADIUS.lg,
    border: `1px solid #FBCFE8`,
    padding: SPACING.px.comfortable,
  }}>
    <div style={{
      display: 'flex',
      alignItems: 'center',
      gap: SPACING.px.tight,
      marginBottom: SPACING.px.base,
    }}>
      <Heart size={24} color="#BE185D" />
      <span style={{
        fontWeight: TYPOGRAPHY.fontWeight.bold,
        fontSize: TYPOGRAPHY.fontSize.lg,
        color: '#9D174D',
      }}>QoE</span>
    </div>
    <div style={{ fontSize: TYPOGRAPHY.fontSize.sm, color: ABYSS[700], marginBottom: SPACING.px.base }}>
      <strong>Process: HOW you engage</strong>
    </div>
    <ul style={{ margin: 0, paddingLeft: SPACING.px.comfortable, color: ABYSS[600], fontSize: TYPOGRAPHY.fontSize.sm }}>
      <li>Attention quality</li>
      <li>Scope management</li>
      <li>Resistance awareness</li>
      <li>Energy preservation</li>
      <li>Completion timing</li>
    </ul>
  </div>
</div>

<InfoBox variant="info">
  <strong>In Practice:</strong> When building a new feature, Wu Wei asks "Am I over-engineering this?",
  MAYA asks "Will users immediately understand this?", and QoE asks "Is my engagement alive or am I forcing?"
  All three questions should have satisfying answers.
</InfoBox>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Validation</h2>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(3, 1fr)',
  gap: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
}}>
  <div style={{
    background: PRIMITIVES.softLinen,
    borderRadius: RADIUS.lg,
    border: `1px solid ${SLATE[200]}`,
    padding: SPACING.px.comfortable,
  }}>
    <h4 style={{
      margin: `0 0 ${SPACING.px.base} 0`,
      fontSize: TYPOGRAPHY.fontSize.md,
      fontWeight: TYPOGRAPHY.fontWeight.semibold,
      color: ABYSS[900],
      display: 'flex',
      alignItems: 'center',
      gap: SPACING.px.tight,
    }}>
      <CheckCircle size={20} color="#22C55E" />
      Wu Wei Success Signals
    </h4>
    <ul style={{ margin: 0, paddingLeft: SPACING.px.comfortable, color: ABYSS[600], fontSize: TYPOGRAPHY.fontSize.sm }}>
      <li>Code feels natural, not forced</li>
      <li>Clear enough for any agent to modify</li>
      <li>No fighting with the architecture</li>
      <li>Existing patterns were reused</li>
      <li>Complexity was reduced, not added</li>
    </ul>
  </div>

  <div style={{
    background: PRIMITIVES.softLinen,
    borderRadius: RADIUS.lg,
    border: `1px solid ${SLATE[200]}`,
    padding: SPACING.px.comfortable,
  }}>
    <h4 style={{
      margin: `0 0 ${SPACING.px.base} 0`,
      fontSize: TYPOGRAPHY.fontSize.md,
      fontWeight: TYPOGRAPHY.fontWeight.semibold,
      color: ABYSS[900],
      display: 'flex',
      alignItems: 'center',
      gap: SPACING.px.tight,
    }}>
      <CheckCircle size={20} color="#22C55E" />
      MAYA Success Signals
    </h4>
    <ul style={{ margin: 0, paddingLeft: SPACING.px.comfortable, color: ABYSS[600], fontSize: TYPOGRAPHY.fontSize.sm }}>
      <li>"Looks fresh but I immediately understood it"</li>
      <li>Quick user adoption</li>
      <li>Low cognitive load</li>
      <li>Familiar interaction patterns</li>
      <li>Modern visuals, stable behaviors</li>
    </ul>
  </div>

  <div style={{
    background: PRIMITIVES.softLinen,
    borderRadius: RADIUS.lg,
    border: `1px solid ${SLATE[200]}`,
    padding: SPACING.px.comfortable,
  }}>
    <h4 style={{
      margin: `0 0 ${SPACING.px.base} 0`,
      fontSize: TYPOGRAPHY.fontSize.md,
      fontWeight: TYPOGRAPHY.fontWeight.semibold,
      color: ABYSS[900],
      display: 'flex',
      alignItems: 'center',
      gap: SPACING.px.tight,
    }}>
      <CheckCircle size={20} color="#22C55E" />
      QoE Success Signals
    </h4>
    <ul style={{ margin: 0, paddingLeft: SPACING.px.comfortable, color: ABYSS[600], fontSize: TYPOGRAPHY.fontSize.sm }}>
      <li>Task continuation feels natural</li>
      <li>Scope was appropriately bounded</li>
      <li>Exploration over brute-force</li>
      <li>Stopped at a coherent point</li>
      <li>Friction signals were investigated</li>
    </ul>
  </div>
</div>

<InfoBox variant="warning">
  <strong>Agent Self-Check:</strong> If users find UI confusing → MAYA violation.
  If implementation feels like a fight → Wu Wei violation.
  If scope keeps growing or you're hammering the same approach → QoE violation.
  All three philosophies guide agents toward the same goal: quality code that serves users.
</InfoBox>
