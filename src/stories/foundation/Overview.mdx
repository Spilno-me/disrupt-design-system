{/* @jsxRuntime automatic */}
{/* @jsxImportSource react */}
import { Meta } from '@storybook/addon-docs/blocks';
import { Palette, Type, Space, Layers, Circle, Sparkles, ArrowRight, BookOpen, GitBranch, Network } from 'lucide-react';
import {
  BrandHero,
  InfoBox,
  SectionHeader,
  ABYSS,
  DEEP_CURRENT,
  DUSK_REEF,
  PRIMITIVES,
  SHADOWS,
  RADIUS,
  SLATE,
  SPACING,
  TYPOGRAPHY,
} from '../brand/BrandComponents';
import { NavCard, CodeBlock, TOKENS } from './DocComponents';

<Meta title="Foundation/Design Tokens/Overview" />

{/* Hero Header */}
<BrandHero
  title="Design Tokens"
  description="The foundational building blocks of the Disrupt Design System. Tokens ensure consistency across all products while enabling scalable, maintainable design."
  gradient="primary"
  decorativeIcon={<BookOpen size={180} color={PRIMITIVES.white} strokeWidth={1} />}
/>

{/* Architecture Overview */}
<div style={{ marginBottom: SPACING.px.section }}>
  <h2 style={{
    fontSize: '24px',
    fontWeight: 700,
    fontFamily: '"Pilat Extended", sans-serif',
    color: ABYSS[500],
    marginBottom: SPACING.px.sectionHeadingBottom,
  }}>
    Token Architecture
  </h2>

  {/* Flow Container */}
  <div style={{
    background: `linear-gradient(135deg, ${SLATE[50]} 0%, ${DEEP_CURRENT[50]} 100%)`,
    borderRadius: RADIUS.xl,
    padding: '32px',
    marginBottom: '24px',
    position: 'relative',
  }}>
    {/* Flow Steps */}
    <div style={{
      display: 'flex',
      alignItems: 'stretch',
      gap: '0',
    }}>
      {/* Step 1: Primitives */}
      <div style={{
        flex: 1,
        background: PRIMITIVES.white,
        borderRadius: RADIUS.lg,
        padding: '24px',
        boxShadow: SHADOWS.sm,
        border: `1px solid ${SLATE[200]}`,
        position: 'relative',
        zIndex: 2,
      }}>
        <div style={{
          display: 'flex',
          alignItems: 'center',
          gap: '12px',
          marginBottom: '12px',
        }}>
          <div style={{
            width: '28px',
            height: '28px',
            borderRadius: RADIUS.full,
            background: DUSK_REEF[100],
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontSize: '13px',
            fontWeight: 700,
            color: DUSK_REEF[600],
            fontFamily: '"Fixel", sans-serif',
          }}>
            <span>1</span>
          </div>
          <h3 style={{
            margin: 0,
            fontSize: '15px',
            fontWeight: 600,
            fontFamily: '"Fixel", sans-serif',
            color: ABYSS[500],
          }}>
            <span>Primitives</span>
          </h3>
        </div>
        <p style={{
          margin: 0,
          fontSize: '13px',
          fontFamily: '"Fixel", sans-serif',
          color: DUSK_REEF[500],
          lineHeight: 1.5,
        }}>
          <span>Raw values — color scales, spacing units, font sizes. The source of truth.</span>
        </p>
        <div style={{
          marginTop: '12px',
          display: 'flex',
          gap: '6px',
        }}>
          <span style={{
            background: ABYSS[500],
            padding: '2px 8px',
            borderRadius: RADIUS.full,
            fontSize: '11px',
            fontFamily: '"JetBrains Mono", monospace',
            color: PRIMITIVES.white,
          }}>ABYSS[500]</span>
          <span style={{
            background: DEEP_CURRENT[700],
            padding: '2px 8px',
            borderRadius: RADIUS.full,
            fontSize: '11px',
            fontFamily: '"JetBrains Mono", monospace',
            color: PRIMITIVES.white,
          }}>DEEP_CURRENT</span>
        </div>
      </div>

      {/* Arrow Connector */}
      <div style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: '0 16px',
        position: 'relative',
        zIndex: 1,
      }}>
        <div style={{
          width: '40px',
          height: '40px',
          borderRadius: RADIUS.full,
          background: PRIMITIVES.white,
          boxShadow: SHADOWS.md,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}>
          <ArrowRight size={20} color={DEEP_CURRENT[500]} strokeWidth={2.5} />
        </div>
      </div>

      {/* Step 2: Alias */}
      <div style={{
        flex: 1,
        background: PRIMITIVES.white,
        borderRadius: RADIUS.lg,
        padding: '24px',
        boxShadow: SHADOWS.md,
        border: `2px solid ${DEEP_CURRENT[500]}`,
        position: 'relative',
        zIndex: 2,
      }}>
        <div style={{
          position: 'absolute',
          top: '-10px',
          right: '16px',
          background: DEEP_CURRENT[700],
          color: PRIMITIVES.white,
          padding: '3px 10px',
          borderRadius: RADIUS.full,
          fontSize: '10px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          textTransform: 'uppercase',
          letterSpacing: '0.5px',
        }}>
          <span>Use This</span>
        </div>
        <div style={{
          display: 'flex',
          alignItems: 'center',
          gap: '12px',
          marginBottom: '12px',
        }}>
          <div style={{
            width: '28px',
            height: '28px',
            borderRadius: RADIUS.full,
            background: DEEP_CURRENT[700],
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontSize: '13px',
            fontWeight: 700,
            color: PRIMITIVES.white,
            fontFamily: '"Fixel", sans-serif',
          }}>
            <span>2</span>
          </div>
          <h3 style={{
            margin: 0,
            fontSize: '15px',
            fontWeight: 600,
            fontFamily: '"Fixel", sans-serif',
            color: ABYSS[500],
          }}>
            <span>Alias (Semantic)</span>
          </h3>
        </div>
        <p style={{
          margin: 0,
          fontSize: '13px',
          fontFamily: '"Fixel", sans-serif',
          color: DUSK_REEF[500],
          lineHeight: 1.5,
        }}>
          <span>What colors <strong>mean</strong>. Use these in components. Always.</span>
        </p>
        <div style={{
          marginTop: '12px',
          display: 'flex',
          gap: '6px',
        }}>
          <span style={{
            background: SLATE[100],
            padding: '2px 8px',
            borderRadius: RADIUS.full,
            fontSize: '11px',
            fontFamily: '"JetBrains Mono", monospace',
            color: ABYSS[500],
          }}>text-primary</span>
          <span style={{
            background: SLATE[100],
            padding: '2px 8px',
            borderRadius: RADIUS.full,
            fontSize: '11px',
            fontFamily: '"JetBrains Mono", monospace',
            color: ABYSS[500],
          }}>bg-surface</span>
        </div>
      </div>
    </div>
  </div>

  <InfoBox variant="tip">
    <strong>Rule:</strong> Components always use <strong>Alias tokens</strong> (Tier 2).
    Never hardcode hex values. This ensures changes propagate automatically across the entire system.
  </InfoBox>
</div>

{/* Quick Reference */}
<div style={{ marginBottom: SPACING.px.section }}>
  <h2 style={{
    fontSize: '24px',
    fontWeight: 700,
    fontFamily: '"Pilat Extended", sans-serif',
    color: ABYSS[500],
    marginBottom: SPACING.px.sectionHeadingBottom,
  }}>
    Quick Reference
  </h2>

  <CodeBlock
    language="tsx"
    code={`// Import tokens in your component
import { ALIAS, SHADOWS, RADIUS } from '@adrozdenko/design-system'

// Use semantic tokens (recommended)
<div style={{
  background: ALIAS.background.surface,
  color: ALIAS.text.primary,
  borderRadius: RADIUS.md,
  boxShadow: SHADOWS.md,
}}>

// Or use Tailwind classes
<div className="bg-surface text-primary rounded-md shadow-md">
  Content
</div>`}
  />
</div>

{/* Navigation Grid */}
<div style={{ marginBottom: SPACING.px.section }}>
  <h2 style={{
    fontSize: '24px',
    fontWeight: 700,
    fontFamily: '"Pilat Extended", sans-serif',
    color: ABYSS[500],
    marginBottom: SPACING.px.sectionHeadingBottom,
  }}>
    Explore Tokens
  </h2>

  <div style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(2, 1fr)',
    gap: '20px',
  }}>
    <NavCard
      title="Colors"
      description="Brand colors, semantic palettes, and usage guidelines for the complete color system."
      href="?path=/docs/foundation-design-tokens-colors--docs"
      icon={<Palette size={24} />}
      color={DEEP_CURRENT[500]}
    />

    <NavCard
      title="Typography"
      description="Font families, sizes, weights, and text styling patterns for web and app."
      href="?path=/docs/foundation-design-tokens-typography--docs"
      icon={<Type size={24} />}
      color={DUSK_REEF[500]}
    />

    <NavCard
      title="Spacing"
      description="Consistent spacing scale for margins, padding, and layout gaps."
      href="?path=/docs/foundation-design-tokens-spacing--docs"
      icon={<Space size={24} />}
      color={ABYSS[400]}
    />

    <NavCard
      title="Shadows"
      description="Elevation system with natural light physics for depth and hierarchy."
      href="?path=/docs/foundation-design-tokens-shadows--docs"
      icon={<Layers size={24} />}
      color={ABYSS[500]}
    />

    <NavCard
      title="Border Radius"
      description="Consistent corner rounding from sharp to fully rounded elements."
      href="?path=/docs/foundation-design-tokens-radius--docs"
      icon={<Circle size={24} />}
      color={DEEP_CURRENT[600]}
    />

    <NavCard
      title="Animations"
      description="Transitions, timing functions, and motion patterns for interactions."
      href="?path=/docs/foundation-design-tokens-animations--docs"
      icon={<Sparkles size={24} />}
      color={DUSK_REEF[400]}
    />

    <NavCard
      title="Token Tree 3D"
      description="Interactive 3D visualization of the complete token hierarchy."
      href="?path=/docs/foundation-design-tokens-token-tree--docs"
      icon={<GitBranch size={24} />}
      color={ABYSS[500]}
    />

    <NavCard
      title="Dependency Graph"
      description="Interactive 2D graph showing component and token dependencies."
      href="?path=/docs/foundation-design-tokens-dependency-graph--docs"
      icon={<Network size={24} />}
      color={DEEP_CURRENT[600]}
    />
  </div>
</div>

{/* Source of Truth */}
<div style={{
  background: ABYSS[50],
  borderRadius: RADIUS.lg,
  padding: '24px',
  border: `1px solid ${ABYSS[100]}`,
}}>
  <h3 style={{
    margin: '0 0 12px 0',
    fontSize: '16px',
    fontWeight: 600,
    fontFamily: '"Fixel", sans-serif',
    color: ABYSS[500],
  }}>
    Source of Truth
  </h3>
  <p style={{
    margin: '0 0 16px 0',
    fontSize: '14px',
    fontFamily: '"Fixel", sans-serif',
    color: DUSK_REEF[500],
    lineHeight: 1.5,
  }}>
    <span>All token values are defined in a single file. Changes here automatically update all documentation and components.</span>
  </p>
  <code style={{
    display: 'block',
    background: ABYSS[700],
    padding: '12px 16px',
    borderRadius: RADIUS.sm,
    fontSize: '13px',
    fontFamily: '"JetBrains Mono", ui-monospace, monospace',
  }}>
    <span style={{ color: PRIMITIVES.white }}>src/constants/designTokens.ts</span>
  </code>
</div>
