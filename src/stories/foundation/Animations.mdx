{/* @jsxRuntime automatic */}
{/* @jsxImportSource react */}
import { Meta } from '@storybook/addon-docs/blocks';
import { Sparkles, Zap, Clock, Play } from 'lucide-react';
import {
  SectionHeader,
  InfoBox,
  TokenTable,
  CodeBlock,
  TOKENS,
} from './DocComponents';
import {
  ABYSS,
  DEEP_CURRENT,
  DUSK_REEF,
  PRIMITIVES,
  SHADOWS,
  RADIUS,
  SLATE,
  TRANSITIONS,
} from '../../constants/designTokens';

<Meta title="Foundation/Design Tokens/Animations" />

{/* Page Header */}
<div style={{
  background: `linear-gradient(135deg, ${DUSK_REEF[400]} 0%, ${DUSK_REEF[600]} 100%)`,
  borderRadius: RADIUS.xl,
  padding: '40px',
  marginBottom: '48px',
}}>
  <div style={{
    display: 'inline-flex',
    alignItems: 'center',
    gap: '8px',
    marginBottom: '12px',
  }}>
    <Sparkles size={28} color={PRIMITIVES.white} />
  </div>

  <h1 style={{
    margin: '0 0 12px 0',
    fontSize: '36px',
    fontWeight: 700,
    fontFamily: '"Pilat Extended", sans-serif',
    color: PRIMITIVES.white,
  }}>
    Animations & Transitions
  </h1>

  <p style={{
    margin: 0,
    fontSize: '16px',
    fontFamily: '"Fixel", sans-serif',
    color: DUSK_REEF[100],
    lineHeight: 1.6,
    maxWidth: '500px',
  }}>
    Motion creates meaning. Consistent timing and easing functions make
    interactions feel natural and responsive.
  </p>
</div>

<InfoBox variant="tip">
  <strong>Rule:</strong> Use shorter durations for small elements (150-200ms),
  longer for large elements (300-500ms). Always use ease-out for user-initiated actions.
</InfoBox>

---

## Transition Durations

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(4, 1fr)',
  gap: '16px',
  marginBottom: '48px',
}}>
  {[
    { name: 'Fast', value: '150ms', usage: 'Micro-interactions, hover states' },
    { name: 'Normal', value: '200ms', usage: 'Standard transitions (DEFAULT)' },
    { name: 'Smooth', value: '300ms', usage: 'Larger element transitions' },
    { name: 'Slow', value: '500ms', usage: 'Page transitions, emphasis' },
  ].map((item) => (
    <div
      key={item.name}
      style={{
        background: PRIMITIVES.white,
        borderRadius: RADIUS.lg,
        padding: '24px',
        border: `1px solid ${SLATE[200]}`,
        boxShadow: SHADOWS.sm,
      }}
    >
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px',
        marginBottom: '12px',
      }}>
        <Clock size={16} color={DEEP_CURRENT[500]} />
        <span style={{
          fontSize: '14px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: ABYSS[500],
        }}>
          {item.name}
        </span>
      </div>
      <div style={{
        fontSize: '24px',
        fontWeight: 700,
        fontFamily: '"Pilat Extended", sans-serif',
        color: DEEP_CURRENT[500],
        marginBottom: '8px',
      }}>
        {item.value}
      </div>
      <div style={{
        fontSize: '12px',
        fontFamily: '"Fixel", sans-serif',
        color: DUSK_REEF[500],
      }}>
        {item.usage}
      </div>
    </div>
  ))}
</div>

---

## Interactive Demos

Hover over these elements to see transitions in action:

### Button Hover Effects

<div style={{
  display: 'flex',
  gap: '16px',
  marginBottom: '48px',
  flexWrap: 'wrap',
}}>
  <button
    style={{
      padding: '12px 24px',
      backgroundColor: DEEP_CURRENT[500],
      color: PRIMITIVES.white,
      border: 'none',
      borderRadius: RADIUS.md,
      cursor: 'pointer',
      fontFamily: '"Fixel", sans-serif',
      fontSize: '14px',
      fontWeight: 500,
      transition: 'all 150ms ease-out',
    }}
    onMouseEnter={(e) => {
      e.currentTarget.style.backgroundColor = DEEP_CURRENT[600]
      e.currentTarget.style.transform = 'translateY(-2px)'
      e.currentTarget.style.boxShadow = SHADOWS.md
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.backgroundColor = DEEP_CURRENT[500]
      e.currentTarget.style.transform = 'translateY(0)'
      e.currentTarget.style.boxShadow = 'none'
    }}
  >
    Fast (150ms)
  </button>

  <button
    style={{
      padding: '12px 24px',
      backgroundColor: DEEP_CURRENT[500],
      color: PRIMITIVES.white,
      border: 'none',
      borderRadius: RADIUS.md,
      cursor: 'pointer',
      fontFamily: '"Fixel", sans-serif',
      fontSize: '14px',
      fontWeight: 500,
      transition: 'all 200ms ease-out',
    }}
    onMouseEnter={(e) => {
      e.currentTarget.style.backgroundColor = DEEP_CURRENT[600]
      e.currentTarget.style.transform = 'translateY(-2px)'
      e.currentTarget.style.boxShadow = SHADOWS.md
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.backgroundColor = DEEP_CURRENT[500]
      e.currentTarget.style.transform = 'translateY(0)'
      e.currentTarget.style.boxShadow = 'none'
    }}
  >
    Normal (200ms)
  </button>

  <button
    style={{
      padding: '12px 24px',
      backgroundColor: DEEP_CURRENT[500],
      color: PRIMITIVES.white,
      border: 'none',
      borderRadius: RADIUS.md,
      cursor: 'pointer',
      fontFamily: '"Fixel", sans-serif',
      fontSize: '14px',
      fontWeight: 500,
      transition: 'all 300ms ease-out',
    }}
    onMouseEnter={(e) => {
      e.currentTarget.style.backgroundColor = DEEP_CURRENT[600]
      e.currentTarget.style.transform = 'translateY(-2px)'
      e.currentTarget.style.boxShadow = SHADOWS.md
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.backgroundColor = DEEP_CURRENT[500]
      e.currentTarget.style.transform = 'translateY(0)'
      e.currentTarget.style.boxShadow = 'none'
    }}
  >
    Smooth (300ms)
  </button>
</div>

### Card Elevation Change

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(2, 1fr)',
  gap: '24px',
  marginBottom: '48px',
}}>
  <div
    style={{
      background: PRIMITIVES.white,
      borderRadius: RADIUS.lg,
      padding: '24px',
      border: `1px solid ${SLATE[300]}`,
      boxShadow: SHADOWS.sm,
      cursor: 'pointer',
      transition: 'all 200ms ease-out',
    }}
    onMouseEnter={(e) => {
      e.currentTarget.style.borderColor = DEEP_CURRENT[500]
      e.currentTarget.style.boxShadow = SHADOWS.md
      e.currentTarget.style.transform = 'translateY(-2px)'
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.borderColor = SLATE[300]
      e.currentTarget.style.boxShadow = SHADOWS.sm
      e.currentTarget.style.transform = 'translateY(0)'
    }}
  >
    <div style={{ fontWeight: 600, marginBottom: '8px', color: ABYSS[500], fontFamily: '"Fixel", sans-serif' }}>
      Quick Transition
    </div>
    <div style={{ fontSize: '14px', color: DUSK_REEF[500], fontFamily: '"Fixel", sans-serif' }}>
      200ms ease-out - Responsive and snappy
    </div>
  </div>

  <div
    style={{
      background: PRIMITIVES.white,
      borderRadius: RADIUS.lg,
      padding: '24px',
      border: `1px solid ${SLATE[300]}`,
      boxShadow: SHADOWS.sm,
      cursor: 'pointer',
      transition: 'all 300ms ease-out',
    }}
    onMouseEnter={(e) => {
      e.currentTarget.style.borderColor = DEEP_CURRENT[500]
      e.currentTarget.style.boxShadow = SHADOWS.elevated
      e.currentTarget.style.transform = 'translateY(-4px)'
    }}
    onMouseLeave={(e) => {
      e.currentTarget.style.borderColor = SLATE[300]
      e.currentTarget.style.boxShadow = SHADOWS.sm
      e.currentTarget.style.transform = 'translateY(0)'
    }}
  >
    <div style={{ fontWeight: 600, marginBottom: '8px', color: ABYSS[500], fontFamily: '"Fixel", sans-serif' }}>
      Smooth Transition
    </div>
    <div style={{ fontSize: '14px', color: DUSK_REEF[500], fontFamily: '"Fixel", sans-serif' }}>
      300ms ease-out - Polished and elegant
    </div>
  </div>
</div>

---

## Timing Functions

<TokenTable
  tokens={[
    { name: 'ease', value: 'ease', tailwind: 'ease', usage: 'General purpose' },
    { name: 'ease-in', value: 'ease-in', tailwind: 'ease-in', usage: 'Elements entering (rare)' },
    { name: 'ease-out', value: 'ease-out', tailwind: 'ease-out', usage: 'User-initiated (RECOMMENDED)' },
    { name: 'ease-in-out', value: 'ease-in-out', tailwind: 'ease-in-out', usage: 'Looping animations' },
  ]}
/>

---

## Common Patterns

<TokenTable
  tokens={[
    { name: 'Color change', value: '200ms', tailwind: 'transition-colors duration-200', usage: 'Hover color changes' },
    { name: 'Transform', value: '300ms', tailwind: 'transition-transform duration-300', usage: 'Position, scale changes' },
    { name: 'Opacity', value: '200ms', tailwind: 'transition-opacity duration-200', usage: 'Fade in/out' },
    { name: 'All', value: '200ms', tailwind: 'transition-all duration-200', usage: 'Multiple property changes' },
    { name: 'Shadow', value: '200ms', tailwind: 'transition-shadow duration-200', usage: 'Elevation changes' },
  ]}
/>

---

## Usage Examples

<CodeBlock
  language="tsx"
  code={`// Button hover
<button className="
  bg-accent-strong
  text-inverse
  transition-all
  duration-200
  ease-out
  hover:bg-accent-strong/90
  hover:-translate-y-0.5
  hover:shadow-md
">
  Hover me
</button>

// Card hover elevation
<div className="
  bg-surface
  shadow-sm
  transition-all
  duration-200
  hover:shadow-elevated
  hover:-translate-y-1
  hover:border-accent
">
  Interactive card
</div>

// Focus ring animation
<input className="
  transition-all
  duration-150
  focus:ring-2
  focus:ring-focus
  focus:border-focus
" />

// Smooth page transitions
<div className="
  transition-opacity
  duration-300
  ease-out
">
  Page content
</div>`}
/>

---

## TypeScript Usage

<CodeBlock
  language="typescript"
  code={`import { TRANSITIONS } from '@adrozdenko/design-system'

const buttonStyle = {
  transition: \`all \${TRANSITIONS.duration.normal} \${TRANSITIONS.timing.easeOut}\`,
}

// Or use the common patterns
const cardStyle = {
  transition: 'all 200ms ease-out',
}`}
/>
