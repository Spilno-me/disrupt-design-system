{/* @jsxRuntime automatic */}
{/* @jsxImportSource react */}
import { Meta } from '@storybook/addon-docs/blocks';
import { Type, Braces } from 'lucide-react';
import {
  BrandHero,
  InfoBox,
  SectionHeader,
  StoryLink,
  ABYSS,
  CORAL,
  DEEP_CURRENT,
  DUSK_REEF,
  PRIMITIVES,
  SHADOWS,
  RADIUS,
  SLATE,
  SPACING,
  TYPOGRAPHY,
} from '../brand/BrandComponents';
import { HARBOR } from '../../constants/designTokens';
import { TokenTable, CodeBlock } from './DocComponents';

<Meta title="Foundation/Design Tokens/Typography" />

{/* Hero Header */}
<BrandHero
  title="App Typography"
  description="Two typefaces for application interfaces: Fixel for all UI text, JetBrains Mono for code and technical content. Together they create clear visual hierarchy."
  gradient="purple"
  decorativeIcon={<Type size={180} color={PRIMITIVES.white} strokeWidth={1} />}
/>

<InfoBox variant="tip">
  <strong>Two fonts:</strong> <strong>Fixel</strong> (font-sans) for all UI text, <strong>JetBrains Mono</strong> (font-mono) for code and technical content only.
  Always use Tailwind classes from the DDS preset. Never hardcode pixel values.
</InfoBox>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Golden Rules of Typography</h2>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(2, 1fr)',
  gap: '16px',
  marginBottom: '48px',
}}>
  {/* Rule 1 */}
  <div style={{
    background: PRIMITIVES.softLinen,
    borderRadius: RADIUS.md,
    padding: '20px',
    border: `1px solid ${SLATE[200]}`,
  }}>
    <div style={{
      display: 'flex',
      alignItems: 'center',
      gap: '12px',
      marginBottom: '12px',
    }}>
      <div style={{
        width: '32px',
        height: '32px',
        borderRadius: RADIUS.full,
        background: DEEP_CURRENT[600],  // [600] for 4.89:1 contrast (AA compliant)
        color: PRIMITIVES.white,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: '14px',
        fontWeight: 700,
        fontFamily: '"Fixel", sans-serif',
      }}>
        <span>1</span>
      </div>
      <h4 style={{
        margin: 0,
        fontSize: '16px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: ABYSS[500],
      }}>
        <span>Visual = Information Hierarchy</span>
      </h4>
    </div>
    <p style={{
      margin: 0,
      fontSize: '14px',
      fontFamily: '"Fixel", sans-serif',
      color: DUSK_REEF[500],
      lineHeight: 1.6,
    }}>
      Most important content should be largest and boldest. Least important should be smallest and lightest.
    </p>
  </div>

  {/* Rule 2 */}
  <div style={{
    background: PRIMITIVES.softLinen,
    borderRadius: RADIUS.md,
    padding: '20px',
    border: `1px solid ${SLATE[200]}`,
  }}>
    <div style={{
      display: 'flex',
      alignItems: 'center',
      gap: '12px',
      marginBottom: '12px',
    }}>
      <div style={{
        width: '32px',
        height: '32px',
        borderRadius: RADIUS.full,
        background: DEEP_CURRENT[600],  // [600] for 4.89:1 contrast (AA compliant)
        color: PRIMITIVES.white,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: '14px',
        fontWeight: 700,
        fontFamily: '"Fixel", sans-serif',
      }}>
        <span>2</span>
      </div>
      <h4 style={{
        margin: 0,
        fontSize: '16px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: ABYSS[500],
      }}>
        <span>Max 3-4 Sizes Per View</span>
      </h4>
    </div>
    <p style={{
      margin: 0,
      fontSize: '14px',
      fontFamily: '"Fixel", sans-serif',
      color: DUSK_REEF[500],
      lineHeight: 1.6,
    }}>
      Too many font sizes create visual chaos. Use Title (24px) → Section (18px) → Body (14px) → Caption (12px).
    </p>
  </div>

  {/* Rule 3 */}
  <div style={{
    background: PRIMITIVES.softLinen,
    borderRadius: RADIUS.md,
    padding: '20px',
    border: `1px solid ${SLATE[200]}`,
  }}>
    <div style={{
      display: 'flex',
      alignItems: 'center',
      gap: '12px',
      marginBottom: '12px',
    }}>
      <div style={{
        width: '32px',
        height: '32px',
        borderRadius: RADIUS.full,
        background: DEEP_CURRENT[600],  // [600] for 4.89:1 contrast (AA compliant)
        color: PRIMITIVES.white,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: '14px',
        fontWeight: 700,
        fontFamily: '"Fixel", sans-serif',
      }}>
        <span>3</span>
      </div>
      <h4 style={{
        margin: 0,
        fontSize: '16px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: ABYSS[500],
      }}>
        <span>Weight Creates Emphasis</span>
      </h4>
    </div>
    <p style={{
      margin: 0,
      fontSize: '14px',
      fontFamily: '"Fixel", sans-serif',
      color: DUSK_REEF[500],
      lineHeight: 1.6,
    }}>
      Don't make text bigger to emphasize it. Use font-semibold or font-medium on same-size text instead.
    </p>
  </div>

  {/* Rule 4 */}
  <div style={{
    background: PRIMITIVES.softLinen,
    borderRadius: RADIUS.md,
    padding: '20px',
    border: `1px solid ${SLATE[200]}`,
  }}>
    <div style={{
      display: 'flex',
      alignItems: 'center',
      gap: '12px',
      marginBottom: '12px',
    }}>
      <div style={{
        width: '32px',
        height: '32px',
        borderRadius: RADIUS.full,
        background: DEEP_CURRENT[600],  // [600] for 4.89:1 contrast (AA compliant)
        color: PRIMITIVES.white,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: '14px',
        fontWeight: 700,
        fontFamily: '"Fixel", sans-serif',
      }}>
        <span>4</span>
      </div>
      <h4 style={{
        margin: 0,
        fontSize: '16px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: ABYSS[500],
      }}>
        <span>Optimal Line Length</span>
      </h4>
    </div>
    <p style={{
      margin: 0,
      fontSize: '14px',
      fontFamily: '"Fixel", sans-serif',
      color: DUSK_REEF[500],
      lineHeight: 1.6,
    }}>
      45-75 characters per line for readability. Use max-w-prose (65ch) for body text.
    </p>
  </div>
</div>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Fixel Typeface</h2>

<div style={{
  background: PRIMITIVES.softLinen,
  borderRadius: RADIUS.lg,
  padding: '32px',
  border: `1px solid ${SLATE[200]}`,
  boxShadow: SHADOWS.md,
  marginBottom: '48px',
}}>
  <div style={{
    display: 'flex',
    alignItems: 'flex-start',
    gap: '12px',
    marginBottom: '24px',
  }}>
    <div style={{
      width: '48px',
      height: '48px',
      borderRadius: RADIUS.md,
      background: DEEP_CURRENT[50],
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      flexShrink: 0,
    }}>
      <Type size={24} color={DEEP_CURRENT[500]} />
    </div>
    <div style={{ display: 'flex', flexDirection: 'column', gap: '2px', paddingTop: '4px' }}>
      <h3 style={{
        margin: 0,
        fontSize: '20px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: ABYSS[500],
        lineHeight: 1.2,
      }}>
        <span>Fixel</span>
      </h3>
      <code style={{
        fontSize: '12px',
        fontFamily: '"JetBrains Mono", ui-monospace, monospace',
        color: SLATE[500],
      }}>
        font-sans (default)
      </code>
    </div>
  </div>

  <p style={{
    margin: '0 0 24px 0',
    fontSize: '14px',
    fontFamily: '"Fixel", sans-serif',
    color: DUSK_REEF[500],
    lineHeight: 1.6,
    maxWidth: '600px',
  }}>
    Clean, readable typeface designed for application interfaces. Optimized for dashboards,
    forms, data tables, and extended reading. Use exclusively for all app UI text.
  </p>

  <div style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(4, 1fr)',
    gap: '16px',
  }}>
    {[
      { weight: 400, name: 'Normal', class: 'font-normal' },
      { weight: 500, name: 'Medium', class: 'font-medium' },
      { weight: 600, name: 'Semibold', class: 'font-semibold' },
      { weight: 700, name: 'Bold', class: 'font-bold' },
    ].map((item) => (
      <div
        key={item.weight}
        style={{
          background: SLATE[50],
          borderRadius: RADIUS.sm,
          padding: '16px',
          textAlign: 'center',
        }}
      >
        <div style={{
          fontFamily: '"Fixel", sans-serif',
          fontSize: '28px',
          fontWeight: item.weight,
          color: ABYSS[500],
          marginBottom: '8px',
        }}>
          <span>Aa</span>
        </div>
        <div style={{
          fontSize: '13px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: ABYSS[500],
          marginBottom: '2px',
        }}>
          <span>{item.name}</span>
        </div>
        <code style={{
          fontSize: '11px',
          fontFamily: '"JetBrains Mono", ui-monospace, monospace',
          color: SLATE[500],
        }}>
          {item.weight}
        </code>
      </div>
    ))}
  </div>
</div>

<InfoBox variant="info">
  <strong>Note:</strong> For brand and marketing typography (including Pilat Extended), see the
  <StoryLink to="/docs/brand-typography--docs" style={{ marginLeft: '4px', color: DEEP_CURRENT[600] }}>Brand Typography</StoryLink> documentation.
</InfoBox>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>JetBrains Mono</h2>

<div style={{
  background: PRIMITIVES.softLinen,
  borderRadius: RADIUS.lg,
  padding: '32px',
  border: `1px solid ${SLATE[200]}`,
  boxShadow: SHADOWS.md,
  marginBottom: '48px',
}}>
  <div style={{
    display: 'flex',
    alignItems: 'flex-start',
    gap: '12px',
    marginBottom: '24px',
  }}>
    <div style={{
      width: '48px',
      height: '48px',
      borderRadius: RADIUS.md,
      background: DEEP_CURRENT[50],
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      flexShrink: 0,
    }}>
      <Braces size={24} color={DEEP_CURRENT[500]} strokeWidth={2} />
    </div>
    <div style={{ display: 'flex', flexDirection: 'column', gap: '2px', paddingTop: '4px' }}>
      <h3 style={{
        margin: 0,
        fontSize: '20px',
        fontWeight: 600,
        fontFamily: '"Fixel", sans-serif',
        color: ABYSS[500],
        lineHeight: 1.2,
      }}>
        <span>JetBrains Mono</span>
      </h3>
      <code style={{
        fontSize: '12px',
        fontFamily: '"JetBrains Mono", ui-monospace, monospace',
        color: SLATE[500],
        lineHeight: 1.2,
      }}>
        font-mono (code only)
      </code>
    </div>
  </div>

  <p style={{
    margin: '0 0 24px 0',
    fontSize: '14px',
    fontFamily: '"Fixel", sans-serif',
    color: DUSK_REEF[500],
    lineHeight: 1.6,
    maxWidth: '600px',
  }}>
    A typeface designed for developers. Optimized for reading code with increased letter height,
    distinctive symbols, and ligature support. Use exclusively for code and technical content.
  </p>

  <div style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(3, 1fr)',
    gap: '16px',
    marginBottom: '24px',
  }}>
    {[
      { weight: 400, name: 'Normal', class: 'font-normal' },
      { weight: 500, name: 'Medium', class: 'font-medium' },
      { weight: 600, name: 'Semibold', class: 'font-semibold' },
    ].map((item) => (
      <div
        key={item.weight}
        style={{
          background: SLATE[50],
          borderRadius: RADIUS.sm,
          padding: '16px',
          textAlign: 'center',
        }}
      >
        <div style={{
          fontFamily: '"JetBrains Mono", ui-monospace, monospace',
          fontSize: '28px',
          fontWeight: item.weight,
          color: ABYSS[500],
          marginBottom: '8px',
        }}>
          <span>Aa</span>
        </div>
        <div style={{
          fontSize: '13px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: ABYSS[500],
          marginBottom: '2px',
        }}>
          <span>{item.name}</span>
        </div>
        <code style={{
          fontSize: '11px',
          fontFamily: '"JetBrains Mono", ui-monospace, monospace',
          color: SLATE[500],
        }}>
          {item.weight}
        </code>
      </div>
    ))}
  </div>

  {/* Character showcase */}
  <div style={{
    background: SLATE[50],
    borderRadius: RADIUS.sm,
    padding: '20px',
  }}>
    <div style={{
      fontFamily: '"JetBrains Mono", ui-monospace, monospace',
      fontSize: '14px',
      color: ABYSS[500],
      lineHeight: 1.8,
    }}>
      <div><span style={{ color: DEEP_CURRENT[500] }}>// Distinctive characters</span></div>
      <div><span>0O 1lI {'{'} {'}'} [ ] ( ) {'=> !== <= >='}</span></div>
      <div style={{ marginTop: '8px' }}><span style={{ color: DUSK_REEF[600] }}>const</span> <span style={{ color: CORAL[500] }}>example</span> = <span style={{ color: HARBOR[600] }}>'Hello, DDS!'</span>;</div>
    </div>
  </div>
</div>

{/* When to use font-mono */}
<div style={{
  background: PRIMITIVES.softLinen,
  borderRadius: RADIUS.lg,
  padding: '24px',
  border: `1px solid ${SLATE[200]}`,
  marginBottom: '48px',
}}>
  <h4 style={{
    margin: '0 0 16px 0',
    fontSize: '16px',
    fontWeight: 600,
    fontFamily: '"Fixel", sans-serif',
    color: ABYSS[500],
  }}>
    <span>When to use font-mono</span>
  </h4>

  <div style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(2, 1fr)',
    gap: '24px',
  }}>
    {/* DO use */}
    <div>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px',
        marginBottom: '12px',
      }}>
        <span style={{
          fontSize: '16px',
          fontWeight: 700,
          color: HARBOR[600],
        }}>✓</span>
        <span style={{
          fontSize: '14px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: HARBOR[700],
        }}>Use for:</span>
      </div>
      <div style={{
        display: 'flex',
        flexDirection: 'column',
        gap: '8px',
      }}>
        {[
          'Code snippets & syntax',
          'API responses & JSON',
          'Token paths (GRADIENTS.heroOverlay)',
          'Technical IDs (TXN-2024-00123)',
          'Timestamps & tabular numbers',
          'Terminal commands',
          'File paths & URLs',
        ].map((item, i) => (
          <div
            key={i}
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: '8px',
              fontSize: '13px',
              fontFamily: '"Fixel", sans-serif',
              color: DUSK_REEF[500],
            }}
          >
            <span style={{ color: HARBOR[500] }}>→</span>
            <span>{item}</span>
          </div>
        ))}
      </div>
    </div>

    {/* DON'T use */}
    <div>
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px',
        marginBottom: '12px',
      }}>
        <span style={{
          fontSize: '16px',
          fontWeight: 700,
          color: CORAL[500],
        }}>✗</span>
        <span style={{
          fontSize: '14px',
          fontWeight: 600,
          fontFamily: '"Fixel", sans-serif',
          color: CORAL[600],
        }}>Never use for:</span>
      </div>
      <div style={{
        display: 'flex',
        flexDirection: 'column',
        gap: '8px',
      }}>
        {[
          'Headings or titles',
          'Body text or paragraphs',
          'Form labels',
          'Button text',
          'Navigation items',
          'Card content',
          'User-facing messages',
        ].map((item, i) => (
          <div
            key={i}
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: '8px',
              fontSize: '13px',
              fontFamily: '"Fixel", sans-serif',
              color: DUSK_REEF[500],
            }}
          >
            <span style={{ color: CORAL[400] }}>→</span>
            <span>{item}</span>
          </div>
        ))}
      </div>
    </div>
  </div>
</div>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Type Scale</h2>

<div style={{
  background: PRIMITIVES.softLinen,
  borderRadius: RADIUS.lg,
  padding: '32px',
  border: `1px solid ${SLATE[200]}`,
  marginBottom: '48px',
}}>
  <div style={{
    display: 'flex',
    flexDirection: 'column',
    gap: '20px',
  }}>
    {[
      { class: 'text-2xl font-sans font-semibold', size: '24px', weight: 600, label: 'Page Title', usage: 'Page headings' },
      { class: 'text-lg font-sans font-semibold', size: '18px', weight: 600, label: 'Section Title', usage: 'Section headings' },
      { class: 'text-base font-sans font-semibold', size: '16px', weight: 600, label: 'Card Title', usage: 'Card titles' },
      { class: 'text-sm font-sans', size: '14px', weight: 400, label: 'Body text for content and descriptions', usage: 'Body text' },
      { class: 'text-sm font-sans font-medium', size: '14px', weight: 500, label: 'Form Label', usage: 'Form labels' },
      { class: 'text-xs font-sans text-muted', size: '12px', weight: 400, label: 'Caption or help text', usage: 'Captions, metadata', muted: true },
    ].map((item, i) => (
      <div
        key={i}
        style={{
          display: 'flex',
          alignItems: 'center',
          gap: '24px',
          padding: '12px 0',
          borderBottom: i < 5 ? `1px solid ${SLATE[100]}` : 'none',
        }}
      >
        <div style={{ flex: 1 }}>
          <div style={{
            fontFamily: '"Fixel", sans-serif',
            fontSize: item.size,
            fontWeight: item.weight,
            color: item.muted ? DUSK_REEF[500] : ABYSS[500],
          }}>
            {item.label}
          </div>
        </div>
        <code style={{
          background: DEEP_CURRENT[50],
          padding: '4px 8px',
          borderRadius: '4px',
          fontSize: '11px',
          fontFamily: '"JetBrains Mono", ui-monospace, monospace',
          color: DEEP_CURRENT[700],
          whiteSpace: 'nowrap',
        }}>
          {item.class}
        </code>
        <div style={{
          fontSize: '12px',
          color: SLATE[500],
          fontFamily: '"Fixel", sans-serif',
          width: '120px',
        }}>
          {item.usage}
        </div>
      </div>
    ))}
  </div>
</div>



