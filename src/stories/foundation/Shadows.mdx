{/* @jsxRuntime automatic */}
{/* @jsxImportSource react */}
import { Meta } from '@storybook/addon-docs/blocks';
import { Layers, Sun, Moon } from 'lucide-react';
import {
  BrandHero,
  InfoBox,
  SectionHeader,
  ABYSS,
  DEEP_CURRENT,
  DUSK_REEF,
  PRIMITIVES,
  SHADOWS,
  RADIUS,
  SLATE,
  SPACING,
  TYPOGRAPHY,
} from '../brand/BrandComponents';
import { CodeBlock } from './DocComponents';

<Meta title="Foundation/Design Tokens/Shadows & Highlights" />

{/* Hero Header */}
<BrandHero
  title="Shadows & Highlights"
  description="Light from above creates shadow below and highlight above. Both effects work together to create realistic depth."
  gradient="primary"
  decorativeIcon={<Layers size={180} color={PRIMITIVES.white} strokeWidth={1} />}
/>

<InfoBox variant="tip">
  <strong>Physics:</strong> Shadow = blocked light (below). Highlight = reflected light (above). Both are needed for realistic depth.
</InfoBox>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Shadow Scale</h2>

<SectionHeader
  icon={<Sun size={20} />}
  title="Light Surfaces"
  description="On light backgrounds, shadows create depth. Use progressively larger shadows for higher elevation."
/>

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(5, 1fr)',
  gap: SPACING.px.cardGap,
  marginBottom: SPACING.px.comfortable,
}}>
  {[
    { name: 'sm', shadow: SHADOWS.sm, use: 'Tags, badges' },
    { name: 'md', shadow: SHADOWS.md, use: 'Cards, buttons' },
    { name: 'elevated', shadow: SHADOWS.elevated, use: 'Featured content' },
    { name: 'lg', shadow: SHADOWS.lg, use: 'Modals, sheets' },
    { name: 'xl', shadow: SHADOWS.xl, use: 'Critical alerts' },
  ].map((item) => (
    <div key={item.name} style={{
      clipPath: `inset(0 -${SPACING.px.spacious} -${SPACING.px.spacious} -${SPACING.px.spacious})`,
    }}>
      <div style={{
        background: PRIMITIVES.softLinen,
        borderRadius: RADIUS.lg,
        padding: SPACING.px.base,
        boxShadow: item.shadow,
        border: `1px solid ${SLATE[200]}`,
      }}>
        <div style={{
          fontSize: TYPOGRAPHY.fontSize.sm[0],
          fontWeight: TYPOGRAPHY.fontWeight.semibold,
          color: ABYSS[500],
          fontFamily: TYPOGRAPHY.fontFamily.sans,
          marginBottom: SPACING.px.micro,
        }}>{item.name}</div>
        <div style={{
          fontSize: TYPOGRAPHY.fontSize.xs[0],
          color: DUSK_REEF[500],
          fontFamily: TYPOGRAPHY.fontFamily.sans,
        }}>{item.use}</div>
      </div>
    </div>
  ))}
</div>

{/* Light surface container */}
<div style={{
  background: SLATE[100],
  borderRadius: RADIUS.lg,
  padding: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
  border: `1px solid ${SLATE[200]}`,
}}>
  {/* White card with shadow */}
  <div style={{
    background: PRIMITIVES.softLinen,
    borderRadius: RADIUS.md,
    padding: SPACING.px.base,
    boxShadow: SHADOWS.md,
    maxWidth: '320px',
  }}>
    <div style={{
      fontSize: TYPOGRAPHY.fontSize.sm[0],
      fontWeight: TYPOGRAPHY.fontWeight.semibold,
      color: ABYSS[500],
      fontFamily: TYPOGRAPHY.fontFamily.sans,
      marginBottom: SPACING.px.micro,
    }}>Card with shadow</div>
    <div style={{
      fontSize: TYPOGRAPHY.fontSize.xs[0],
      color: DUSK_REEF[500],
      fontFamily: TYPOGRAPHY.fontFamily.sans,
    }}>SHADOWS.md on SLATE[100] surface</div>
  </div>
</div>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Highlight</h2>

<SectionHeader
  icon={<Moon size={20} />}
  title="Dark Surfaces"
  description="On dark backgrounds, highlights create depth. Use a gradient that fades from 2 steps lighter at top."
/>

{/* Dark surface container: Surface = ABYSS[700] */}
<div style={{
  background: ABYSS[700],
  borderRadius: RADIUS.lg,
  padding: SPACING.px.comfortable,
  marginBottom: SPACING.px.section,
  border: `1px solid ${ABYSS[600]}`,
}}>
  {/* Card on surface: Card = ABYSS[500] with highlight gradient */}
  <div style={{
    background: `linear-gradient(180deg, ${ABYSS[200]} 0%, ${ABYSS[500]} 5%, ${ABYSS[500]} 100%)`,
    borderRadius: RADIUS.lg,
    padding: 1,
    maxWidth: '320px',
  }}>
    <div style={{
      background: `linear-gradient(180deg, ${ABYSS[300]} 0%, ${ABYSS[500]} 12%)`,
      borderRadius: RADIUS.md,
      padding: SPACING.px.base,
    }}>
      <div style={{
        fontSize: TYPOGRAPHY.fontSize.sm[0],
        fontWeight: TYPOGRAPHY.fontWeight.semibold,
        color: PRIMITIVES.white,
        fontFamily: TYPOGRAPHY.fontFamily.sans,
        marginBottom: SPACING.px.micro,
      }}>Card with highlight</div>
      <div style={{
        fontSize: TYPOGRAPHY.fontSize.xs[0],
        color: SLATE[400],
        fontFamily: TYPOGRAPHY.fontFamily.sans,
      }}>Gradient: ABYSS[300] → ABYSS[500]</div>
    </div>
  </div>
</div>

---

<h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Usage</h2>

<CodeBlock
  language="tsx"
  code={`import { SHADOWS, ABYSS } from '@adrozdenko/design-system'

// Light surface - use shadow for depth
const lightCard = {
  boxShadow: SHADOWS.md,
}

// Dark surface - use gradient highlight (2 steps lighter at top)
const darkCard = {
  background: \`linear-gradient(180deg, \${ABYSS[300]} 0%, \${ABYSS[500]} 12%)\`,
}`}
/>
