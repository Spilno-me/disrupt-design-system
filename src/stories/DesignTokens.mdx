{/* @jsxRuntime automatic */}
{/* @jsxImportSource react */}
import { Meta } from '@storybook/addon-docs/blocks';
import { BookOpen, Sparkles, Palette, Type, Space, Layers, Circle } from 'lucide-react';
import {
  BrandHero,
  ABYSS,
  DEEP_CURRENT,
  DUSK_REEF,
  PRIMITIVES,
  RADIUS,
  SLATE,
  SPACING,
} from './brand/BrandComponents';
import { QuickNav } from './foundation/DocComponents';

<Meta title="Foundation/Design Tokens" />

{/* Hero Header */}
<BrandHero
  title="Design Tokens"
  description="The foundational building blocks of the Disrupt Design System. Tokens ensure consistency across all products while enabling scalable, maintainable design."
  gradient="primary"
  decorativeIcon={<BookOpen size={180} color={PRIMITIVES.white} strokeWidth={1} />}
/>

<QuickNav
  title="Documentation Sections"
  columns={3}
  items={[
    { icon: <BookOpen size={20} />, title: 'Overview', description: 'Architecture & Quick Start', path: '?path=/docs/foundation-design-tokens-overview--docs' },
    { icon: <Palette size={20} />, title: 'Colors', description: 'Full color system', path: '?path=/docs/foundation-design-tokens-colors--docs' },
    { icon: <Type size={20} />, title: 'Typography', description: 'Fonts & text styles', path: '?path=/docs/foundation-design-tokens-typography--docs' },
    { icon: <Space size={20} />, title: 'Spacing', description: 'Layout & whitespace', path: '?path=/docs/foundation-design-tokens-spacing--docs' },
    { icon: <Layers size={20} />, title: 'Shadows', description: 'Elevation system', path: '?path=/docs/foundation-design-tokens-shadows--docs' },
    { icon: <Circle size={20} />, title: 'Radius', description: 'Border radius scale', path: '?path=/docs/foundation-design-tokens-radius--docs' },
    { icon: <Sparkles size={20} />, title: 'Animations', description: 'Motion & transitions', path: '?path=/docs/foundation-design-tokens-animations--docs' },
  ]}
/>

<div>
  <h2 style={{ marginTop: SPACING.px.sectionHeadingTop, marginBottom: SPACING.px.sectionHeadingBottom }}>Single Source of Truth</h2>

  <p style={{
    margin: 0,
    fontSize: '14px',
    fontFamily: '"Fixel", sans-serif',
    color: DUSK_REEF[500],
    lineHeight: 1.6,
  }}>
    <span>All documentation is <strong>automatically synced</strong> with </span>
    <code style={{
      background: SLATE[100],
      padding: '2px 6px',
      borderRadius: RADIUS.xs,
      fontSize: '13px',
      fontFamily: '"JetBrains Mono", monospace',
      color: ABYSS[500],
    }}>designTokens.ts</code>
    <span>. This ensures:</span>
  </p>

  <ul style={{
    margin: '16px 0 0 0',
    padding: '0 0 0 20px',
    fontSize: '14px',
    fontFamily: '"Fixel", sans-serif',
    color: DUSK_REEF[500],
    lineHeight: 1.8,
  }}>
    <li><strong style={{ color: ABYSS[500] }}>100% accuracy</strong> – Documentation always reflects the actual token values</li>
    <li><strong style={{ color: ABYSS[500] }}>No drift</strong> – Changes to tokens automatically update documentation</li>
    <li><strong style={{ color: ABYSS[500] }}>Developer confidence</strong> – Copy code examples knowing they're correct</li>
  </ul>
</div>
