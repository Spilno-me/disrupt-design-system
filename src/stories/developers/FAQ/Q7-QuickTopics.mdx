{/* @jsxRuntime automatic */}
{/* @jsxImportSource react */}
import { Meta } from '@storybook/addon-docs/blocks';
import { Zap, Shield, ArrowLeft } from 'lucide-react';
import { BrandHero, InfoBox, SectionHeader, GuidelineCard, CodeBlock, ABYSS, DEEP_CURRENT, DUSK_REEF, CORAL, HARBOR, SUNRISE, PRIMITIVES, SLATE, RADIUS, SPACING } from '../../brand/BrandComponents';

<Meta title="Developers/FAQ/Q7-QuickTopics" />

<div style={{ marginBottom: '24px' }}>
  <a href="/docs/developers-faq-overview--docs" style={{ display: 'inline-flex', alignItems: 'center', gap: '6px', fontSize: '13px', color: DEEP_CURRENT[600], textDecoration: 'none' }}>
    <ArrowLeft size={14} /> Back to FAQ Overview
  </a>
</div>

<BrandHero
  title="Q7: Quick Topics"
  description="Rapid-fire answers to common questions about security, customization, cost, and governance."
  gradient="purple"
  decorativeIcon={<Zap size={180} color={PRIMITIVES.white} strokeWidth={1} />}
/>

<div style={{ background: CORAL[50], borderRadius: RADIUS.xl, padding: '24px', border: `1px solid ${CORAL[200]}`, marginBottom: '24px' }}>
  <h4 style={{ margin: '0 0 12px 0', fontSize: '15px', fontWeight: 600, color: CORAL[700] }}>The Concerns</h4>
  <p style={{ margin: 0, fontSize: '14px', color: DUSK_REEF[600], fontStyle: 'italic' }}>
    {"\"Where does my data go? Can I customize the look? How much does this cost? Who decides what gets added?\""}
  </p>
</div>

---

## Data Security

<div style={{
  background: HARBOR[50],
  borderRadius: RADIUS.lg,
  padding: '24px',
  border: `1px solid ${HARBOR[200]}`,
  marginBottom: '24px',
}}>
  <h4 style={{ margin: '0 0 12px 0', fontSize: '15px', fontWeight: 600, color: HARBOR[700] }}>
    <Shield size={16} style={{ marginRight: '8px', verticalAlign: 'middle' }} />
    DDS Never Sees Your Data
  </h4>
  <p style={{ margin: '0 0 16px 0', fontSize: '13px', color: DUSK_REEF[500] }}>
    {"DDS components run entirely client-side. Your data never passes through DDS infrastructure."}
  </p>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '12px' }}>
    {[
      { label: 'DDS receives', value: 'Nothing' },
      { label: 'DDS stores', value: 'Nothing' },
      { label: 'DDS logs', value: 'Nothing' },
    ].map((item, idx) => (
      <div key={idx} style={{
        background: PRIMITIVES.white,
        padding: '12px',
        borderRadius: RADIUS.md,
        textAlign: 'center',
      }}>
        <div style={{ fontSize: '11px', color: DUSK_REEF[500] }}>{item.label}</div>
        <div style={{ fontSize: '14px', fontWeight: 600, color: HARBOR[700] }}>{item.value}</div>
      </div>
    ))}
  </div>
  <p style={{ margin: '16px 0 0 0', fontSize: '12px', color: DUSK_REEF[500] }}>
    {"Your API calls go directly from your app to your backend. DDS is a UI library, not a service."}
  </p>
</div>

---

## Customization Escape Hatch

<div style={{
  background: CORAL[50],
  borderRadius: RADIUS.lg,
  padding: '24px',
  border: `1px solid ${CORAL[200]}`,
  marginBottom: '24px',
}}>
  <h4 style={{ margin: '0 0 12px 0', fontSize: '15px', fontWeight: 600, color: CORAL[700] }}>
    Short Answer: No
  </h4>
  <p style={{ margin: '0 0 16px 0', fontSize: '13px', color: DUSK_REEF[500] }}>
    {"DDS styling is not customizable by consumers. This is intentional."}
  </p>
  <div style={{
    background: PRIMITIVES.white,
    padding: '16px',
    borderRadius: RADIUS.md,
    marginBottom: '16px',
  }}>
    <div style={{ fontSize: '13px', fontWeight: 600, color: ABYSS[500], marginBottom: '8px' }}>Why No Customization?</div>
    <p style={{ margin: 0, fontSize: '12px', color: DUSK_REEF[500] }}>
      {"Customization breaks consistency, which is the entire point of a design system. If you need a button that's 47px tall with a gradient, that's not a DDS button. Build it yourself."}
    </p>
  </div>
  <InfoBox variant="tip">
    <strong>Exception:</strong> If your customization need is shared by 2+ products, propose it to DDS.
    We'll either add it as a variant or explain why it doesn't fit.
  </InfoBox>
</div>

---

## Cost Model

<div style={{
  background: DEEP_CURRENT[50],
  borderRadius: RADIUS.lg,
  padding: '24px',
  border: `1px solid ${DEEP_CURRENT[200]}`,
  marginBottom: '24px',
}}>
  <h4 style={{ margin: '0 0 12px 0', fontSize: '15px', fontWeight: 600, color: DEEP_CURRENT[700] }}>
    DDS Is Internal Infrastructure
  </h4>
  <p style={{ margin: '0 0 16px 0', fontSize: '13px', color: DUSK_REEF[500] }}>
    {"DDS is an internal Disrupt Inc initiative, not a product."}
  </p>
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '12px' }}>
    {[
      { label: 'Usage cost', value: 'Free' },
      { label: 'Licensing fees', value: 'None' },
      { label: 'Support', value: 'Included' },
    ].map((item, idx) => (
      <div key={idx} style={{
        background: PRIMITIVES.white,
        padding: '12px',
        borderRadius: RADIUS.md,
        textAlign: 'center',
      }}>
        <div style={{ fontSize: '11px', color: DUSK_REEF[500] }}>{item.label}</div>
        <div style={{ fontSize: '14px', fontWeight: 600, color: DEEP_CURRENT[700] }}>{item.value}</div>
      </div>
    ))}
  </div>
  <p style={{ margin: '16px 0 0 0', fontSize: '12px', color: DUSK_REEF[500] }}>
    {"This is infrastructure investment, not a product. For Disrupt teams, there are no charges."}
  </p>
</div>

---

## Governance: Who Decides What Goes In?

<div style={{
  background: PRIMITIVES.white,
  borderRadius: RADIUS.lg,
  padding: '24px',
  border: `1px solid ${SLATE[200]}`,
  marginBottom: '24px',
}}>
  <h4 style={{ margin: '0 0 16px 0', fontSize: '15px', fontWeight: 600, color: ABYSS[500] }}>
    DDS Architect Has Final Say
  </h4>
  <p style={{ margin: '0 0 16px 0', fontSize: '13px', color: DUSK_REEF[500] }}>
    {"What enters the design system is decided by the DDS architect based on these criteria:"}
  </p>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', marginBottom: '16px' }}>
    {[
      'Is it needed by 2+ products?',
      'Does it follow established patterns?',
      'Can DDS maintain it long-term?',
    ].map((item, idx) => (
      <div key={idx} style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
        <div style={{
          width: '20px',
          height: '20px',
          borderRadius: RADIUS.full,
          background: DEEP_CURRENT[500],
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          color: PRIMITIVES.white,
          fontSize: '11px',
          fontWeight: 700,
          flexShrink: 0,
        }}>{idx + 1}</div>
        <span style={{ fontSize: '13px', color: ABYSS[500] }}>{item}</span>
      </div>
    ))}
  </div>
  <InfoBox variant="info">
    <strong>Escalation:</strong> If you disagree with a decision, escalate to Engineering leadership.
    DDS is not a democracy, but it's not a dictatorship either.
  </InfoBox>
</div>

---

## Summary

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(2, 1fr)',
  gap: '16px',
  marginBottom: '24px',
}}>
  <GuidelineCard type="do" title="Trust the security model">
    DDS is client-side only. Your data stays with you.
  </GuidelineCard>

  <GuidelineCard type="do" title="Propose shared needs">
    If 2+ products need something, it's a candidate for DDS.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Expect custom styling">
    Consistency is the point. Build one-offs yourself.
  </GuidelineCard>

  <GuidelineCard type="dont" title="Assume cost surprises">
    DDS is internal infrastructure. No hidden fees.
  </GuidelineCard>
</div>

---

<div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '16px 0', borderTop: `1px solid ${SLATE[200]}` }}>
  <a href="/docs/developers-faq-q6-performance--docs" style={{ fontSize: '13px', color: DEEP_CURRENT[600], textDecoration: 'none' }}>← Q6: Performance</a>
  <a href="/docs/developers-faq-q8-dds-fails--docs" style={{ fontSize: '13px', color: DEEP_CURRENT[600], textDecoration: 'none' }}>Next: Q8: What If DDS Fails →</a>
</div>
